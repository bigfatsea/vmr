<!-- Ver 2026-08-04, by Sonnet 5 -->

# VMR 项目复盘：一个纯 Vibe Coding 项目的 27 天

**复盘对象**：vmr（Virtual Model Router），2026-07-07 至 2026-08-02，106 个 commit
**项目性质**：全部代码、测试、脚本、文档由 Code Agent 产出；人只做需求、设计、方案、决策
**复盘目的**：提炼可迁移到新项目、新技术领域的经验——正面经验（管用的方法）与负面经验（踩过的坑）都在内。本文只记经验，不追踪 vmr 自身问题的当前解决状态；那是 `docs/OUTSTANDING_ISSUES_opus-5.md` 专门的职责。

---

## 0. 这份复盘的证据来源与局限

**用到的证据**：

- 完整 git history（106 commit，含已删除文件的历史内容）
- 设计文档的全部版本（通过 `git log --follow` 追溯 rename 血缘，包括已从工作区消失的 v1/v2/v3）
- 现存文档：两份设计文档、UserGuide 双语、OUTSTANDING_ISSUES、`_tmp/` 下 9 份工作文档、`docs/future-strategy/`、`archived/distribution-strategy/`
- 2026-08-03 产出的一份独立评审（不以设计文档为准绳）——结论已按 3.5 节所述的模式落进永久载体，评审文档本身按惯例已删除
- 代码本身的统计（行数、测试数、覆盖率、依赖）

**局限，必须先说清楚**：

1. **对话历史全部丢失。** 人与 agent 之间真正发生了什么——提了什么需求、否掉了什么方案、纠正了几次——没有任何记录。本文的一切关于"人做了什么"的推断，都是从 commit、文档 diff、以及文档里被记录下来的决策理由**反推**的。
2. **项目 memory 目录是空的。** `~/.claude/projects/-Volumes-SSD2T-code-vmr/memory/` 无任何文件。这本身就是一条发现：这个项目的知识全部沉淀在**仓库内**（设计文档 / CLAUDE.md / 注释 / 测试），没有依赖任何 agent 侧的私有记忆。事后看这是对的——仓库内的知识可以被任何模型、任何工具读到，agent 侧的 memory 不能。
3. **大量中间文档被主动删除。** 至少 20 份评审/分析文档创建后在几天内被删。它们的内容读不到了，但**创建与删除的时间点本身**是可读的，而且这个"用完即删"的模式恰恰是本项目最重要的工作方法之一（见 3.5）。

---

## 1. 事实基线

| 维度 | 数值 |
| --- | --- |
| 周期 | 27 天（2026-07-07 → 2026-08-02），无一天间断超过 1 天 |
| commit | 106 个，日均 3.9 个，峰值 16 个/天（D2） |
| 生产代码 | 25,574 行 Go |
| 测试代码 | 23,959 行 Go（测试/生产 = **0.94**） |
| 测试函数 | 758 个（750 Test + 6 Benchmark + 2 Fuzz） |
| 文档 | 8,833 行 Markdown（不含市场类） |
| Shell | 685 行（`vmr.sh` 609 + `vmr-loadtest.sh` 76） |
| 直接依赖 | **4 个**（fsnotify、klauspost/compress、x/image、yaml.v3） |
| TODO/FIXME/HACK | **0 处** |
| internal 包 | 22 个 |
| 参与模型 | Fable 5（13 commit 署名）、Sonnet 5（33）、Opus 5（1）、另有 Gemini 3.6 Flash / DeepSeek V4 Pro 参与文档与评审 |
| 覆盖率 | 多数包 82%–98%，最低 i18n 17.3% |

**成品能力**：三协议入口（OpenAI Chat Completions / Anthropic Messages / OpenAI Responses）的本地 LLM 路由器，带健康状态机、条件路由、会话粘性、图片降采样、审计日志，加两个离线分析工具（`vmr report` 聚合报表、`vmr story` 任务叙事重建）。已发布 GitHub Release + Homebrew tap。

---

## 2. 编年史：六个阶段

### 阶段 0 — 一页纸（D1 凌晨，人独立完成）

第一个 commit `f494ae0` 里有两份文档：`VirtualModelRouter.md`（172 行）和 `VirtualModelRouter_v2_Fable5.md`（305 行）。20 秒后的第二个 commit 把前者删掉了。

**前者是人自己写的。** 它是整个项目唯一一份没有 agent 参与的产物，也是最重要的一份。它的结构值得逐条看：

- **定位**（一句话 + 一个类比："定位类似于 nginx、caddy、frp、rclone"）
- **问题定义**（为什么现有方案不行）
- **核心抽象**（恰好 4 个：Virtual Model / Endpoint / Strategy / Health）
- **功能边界**（做什么的清单 + **明确排除清单**：Dashboard、Database、User Management、Billing、Analytics Platform、Prompt Management、Workflow、Plugin System、MCP Framework、企业级 AI Gateway，"一律不做"）
- **架构原则**（4 条长期不变的）
- **技术选型**（Go，并写明"原因不是追求极致性能，而是追求部署体验和长期维护成本"）
- **开发策略**（MVP，第一阶段只做什么）
- **完成标准**（一段可验收的描述）

零行代码，零个函数名，零个数据结构。

**这份文档里有两条后来被证明是错的**：

> "Provider 协议适配应优先复用官方 SDK 或成熟 Adapter"
> "Router 内部应定义一套稳定的 Canonical Request / Response，对外部协议变化保持隔离"

这两条是标准的、听起来很专业的架构建议。它们在 D2 被彻底推翻——最终方案是**零 SDK、零中间表示、字节级透传**。这个事实非常重要，3.6 节会单独讲。

### 阶段 1 — 11 小时到 MVP（D1 白天）

`df038c9` "mvp"：3,237 行一次性落地，含 `cmd/vmr` + 7 个 internal 包 + 5 个测试文件。生产 1,593 行 / 测试 1,046 行。

同一 commit 里还有两份 agent 产出的文档：

- `DEV_PLAN.md` — 一份 M0–M12 的执行清单，每个里程碑都有勾选框，而且**已经全部打勾**。它不是"计划"，是"计划兼进度跟踪兼完工报告"。里面有这样的条目："验收 4：流式逐块转发（MiniMax-M2 SSE 实测）；本地 mock 实测 TTFB 开销中位数 ≈ 0.11ms（直连 0.257ms vs 经代理 0.366ms），远低于 5ms"。
- `IMPLEMENTATION_REPORT_Fable5.md` — 实现总结，含决策点与备选方案对比。

两份都在 D2 被删除。

**这一天的信息量**：MVP 不是"能跑就行"，它从第一分钟起就带 1,046 行测试、带真实 provider 的端到端验证、带对照验收标准的逐条核对。测试/生产比 0.66 起步，之后一路涨到 0.94 并稳定。**这个比例不是后补的，是初始条件。**

### 阶段 2 — 第一次范式反转："直接等价"（D2）

D2 是全项目 commit 最密集的一天（16 个）。核心是 `de0fe34` "Direct-equivalence overhaul"。

它推翻的东西：

- **中间表示（Canonical Request/Response）** → 协议原生格式，永不翻译
- **官方 SDK** → 零 SDK
- **响应处理的第三版实现**

第三条尤其有价值，因为设计文档把三代方案的失败原因逐条记了下来：

> v1 的「200 字节 carry + 字节级状态机」换了 4 个版本都有 corner case（carry 装不下 3000+ 字节 think 块、IN_THINK 时 input 字节丢失、flushTail 重入吐残留）；v2 的「全量缓冲 + 单遍 regex」正确但假流式——TTFB=完整生成时长，逼近 OpenAI SDK 的 `X-Stainless-Timeout: 120` 预算（实测 97K prompt 的请求生成 59s）；v3（现行）以**完整 SSE 事件**为处理单位……**状态机的复杂度病灶在「字节级切分」，不在「模式切换」**。

一个功能，六个版本（状态机 4 版 + 全缓冲 + 事件粒度），失败原因全部留在文档里。

从这天起，"字节忠实透传"成为整个项目的单一核心洞察，此后 27 天没有再动摇过。

### 阶段 3 — 从路由器变成飞行记录仪（D3 – D10）

审计日志在 D1 就有了（`7ad9a3b`），`vmr report` 在 D1 晚上就有雏形（`1e24688`）。但这一阶段它开始压过路由本身的分量：

- D5 `41390ab` "Add agent session analysis and richer dimensions to vmr report" — 生产代码从 6,108 跳到 7,654
- D7 `54e02e2` "Add vmr diagnose and vmr replay commands" — 一次加 1,017 行生产 + 1,317 行测试
- D10 `1d50611` "add load testing"

**这是一次未经宣告的定位漂移的开始。** 人最初写的定位是"LLM 路由器"，排除清单里明确写着"Analytics Platform 不做"。到项目结束时，分析层（report/story/ctxgraph/chatmsg/i18n）的生产代码约 12,000 行，已经**超过**路由核心的约 9,000 行。README 在 D25 被重写，第一句话从"透明路由器"改成 **"The flight recorder for AI agents that run unattended"**。

这次漂移是好的——它带来了这个项目真正的差异化。但它是**事后被承认的**，不是当时决定的。本文后面"负面经验"一节会讲这个代价。

### 阶段 4 — 外部评审驱动的整顿（D11 – D20）

这一阶段的特征是：**大量评审文档被创建，用完立刻删除，结论沉淀进永久载体。**

时间线（创建 → 删除）：

| 文档 | 生命周期 |
| --- | --- |
| `AUDIT_REPORT.md` | 07-16 → 07-17 |
| `AUDIT_REPORT_2_Fable5.md` / `AUDIT_SUMMARY_2_Fable5.md` | 07-17 → 07-17（当天） |
| `audit_report_v2_logs_pi_agent.md` / `..._summary_pi_agent.md` | 07-17 → 07-17（当天） |
| `AUDIT_REPORT_V3_Sonnet5.md` | 07-17 → 07-17（当天） |
| `CodebaseReview_2026-07-12_Fable5.md` | 07-13 → 07-13 |
| `ProjectReview_Fable5.md` | 07-12 → 07-12 |
| `vmr_architecture_review_opus-5.md` | 07-26 → 07-26 |
| `failed_requests_analysis_sonnet-5.md` | 07-26 → 07-26 |
| `VMR_全项目深度审查报告.md` | 07-28 → 07-28 |
| `code-review-step1-*.md`（2 份） | 07-29 → 07-29 |

一天之内出现**四份**独立审计报告（07-17，来自 Fable 5、Sonnet 5、和一个第三方 agent），全部当天处理完当天删除。这不是混乱，是刻意的：**评审报告是消耗品。**

这一阶段的两个永久产物：

**`internal/archtest` 包**（D20，`a9eee76`）。它的包注释是全项目最好的一句话：

> a documented tripwire with no automated check is a tripwire nobody actually sees trip.
> （一个没有自动检查的绊线，是一根没人真正看见它被绊到的绊线。）

它的诞生原因写在代码注释里：`router.go` 从设计文档规定的 ~550 行预算长到了 948 行，**没人发现，因为预算只活在一句注释里**。archtest 把它变成一个会失败的测试。而且注释里连规避手段都堵上了：

> The limit here is set with headroom above that real post-split number, not at the design doc's original round figure — the point is catching regrowth back toward 948, not fighting over every line.
> （超了就拆文件，不要改这个数字。）

**`CLAUDE.md`**（同一天诞生，D20）。这是"给 AI 助手的项目简报"：定位一段、构建命令、22 个包各自负责什么、**不能破坏的不变量清单**、写代码的约定。它诞生在**第 20 天**，不是第 1 天——这一时机是否合适，本文后面会讨论。

### 阶段 5 — 叙事重建（D22 – D24）

`vmr story` 的诞生（`baac448`，D23）：一次 commit 加 2,003 行生产 + 2,273 行测试，带来 3 个新包（`chatmsg` / `ctxgraph` / `story`）。

这是全项目技术上最难的部分，也是方法上最值得看的部分——因为它**完全建立在真实语料的实测之上**：

> 只读末轮消息丢失 26%–99% 的内容（6 个真实会话实测）
> 95.86% 的相邻编辑是纯追加，真正的断裂只占约 1%，但集中在最长、最值得精读的会话里
> 一次真实压缩案例逐位复现：净工作时长手算 381.0s/18.4%，代码复算 380.983s/18.40%
> 全语料 406,534 个 tool_call/tool_result 配对，零孤儿

阈值不是拍脑袋定的，是在 7,112 条记录 / 809K 条消息实例 / 752 会话上校准的。**而没有校准过的那两个阈值，被诚实地写进了"已知限制"表**：

> `stitch.go` 的两档阈值：初始值，只在 2026-07-14..28 这批语料上验证过分布是否合理……语料规模变大时应该重新跑一遍分布检查，而不是假定当前值继续成立。

同一阶段，设计文档从 v3 拆成 v4 双分册（Core 路由核心 / Analytics 报表叙事），因为一份文档已经装不下两个领域。

### 阶段 6 — 收敛与发布（D25 – D27）

- D25：CI + Release workflow、README 重定位为"flight recorder"、`examples/sample-audit.jsonl`（README 里展示的输出是真实可复现的，不是 mockup）
- D25：Homebrew tap 上线
- D27：多语言输出、`openai-responses` 第三协议、**删掉两层从未被启用的配置抽象**（`b037a23` "drop unused passive/global-default layers"）

最后一天做的是**减法**。这值得单独记一笔。

---

## 3. 正面经验：这套方法里真正管用的东西

以下 8 条按"可迁移性"排序，第 1 条最通用。

### 3.1 人写"定位与边界"，agent 写一切实现

那份 172 行的 v1 文档是整个项目的地基。它的关键不在于写了什么，而在于**明确排除清单**——一份"绝不做"的名单。

**为什么这是最高杠杆的一件事**：Code Agent 的默认倾向是**增加**。你说"要个报表"，它会给你分组、给你趋势、给你导出、给你配置项。没有排除清单，一个 MVP 会在两周内长成一个平台。vmr 27 天里始终是单二进制、零数据库、零 Web UI、4 个依赖——不是因为 agent 克制，是因为第一天就有一张写死的排除清单。

**给技术背景不强的人的具体做法**：你不需要懂 Go 或 HTTP。你需要能回答五个问题：

1. 一句话说清这是什么（最好带一个类比："像 nginx"）
2. 现在的痛点是什么，为什么现有工具不行
3. 这个系统里最少有哪几个概念（3–5 个，不能更多）
4. **有哪些看起来相关但坚决不做**（这份最长，也最有价值）
5. 做完之后，用户执行哪一条命令、看到什么，才算成功

这五个问题不涉及任何技术知识，但它们决定了项目 80% 的走向。

### 3.2 设计文档是主干，代码是产物

设计文档的完整血缘：

```
VirtualModelRouter.md          172 行  人写          D1  （20 秒后删除）
VirtualModelRouter_v2_Fable5   305 行  Fable 5 扩写  D1
   ↓ 持续增长 305 → 741 行（21 天）
System_Design_v3               861 行  重写合并      D18 （合并 3 份专题文档）
   ↓ 861 → 898 行
Design_v4_Core                 894 行  拆分          D23
Design_v4_Analytics            376 行  拆分          D23
```

**三个可复制的做法**：

1. **文档与代码在同一个 commit 里更新。** 查 `git log --follow` 可以看到，几乎每个特性 commit 都同时改了设计文档。文档不是事后补的。
2. **文档会被重写，不只是追加。** D18 的 v3 是一次真正的重写（把三份专题文档合并进主文档）；D23 的 v4 是一次拆分。**只追加不重写的文档，两个月后没人读。**
3. **文档有版本头**（`<!-- Ver 2026-08-02 10:54, by Sonnet 5 -->`），标明谁在什么时候改的。

### 3.3 "决策与取舍表" + "决定不修表" — 这套方法的核心发明

设计文档里有两张表，我认为它们是这个项目能跨 27 天、跨至少 5 个不同模型保持一致性的**主要原因**。

**决策与取舍表**：三列——决策 / 备选 / 取舍逻辑。样例：

| 决策 | 备选 | 取舍逻辑 |
| --- | --- | --- |
| Canonical 格式 = 协议原生格式，透传不翻译 | 自研中间表示（IR） | IR 意味着永远追各家新字段，是 LiteLLM 复杂度失控的根源；透传对新参数天然前向兼容 |
| 错误分类含 body 嗅探 | 严格按 HTTP status 映射 | 实测各家 status 习惯不一（400 当 404 用等）；**漏判 = 永不 failover，误判只是一次无害切换** |
| 内容合规错误：切换但零惩罚 | 当普通 4xx 处理 | 该错误按请求不按端点：不切换会中断长程任务，惩罚健康会让一条敏感请求打掉整个端点 |

**决定不修表**：三列——项 / 现状 / **不动的理由**。开头一句话说明它为什么存在：

> 已识别、但判定"动它的收益低于扰动成本"的项。每项都不是 bug，改与不改行为一致；**列在这里是为了下次有人盯着它们犹豫时不必重新论证。**

**为什么这两张表对 vibe coding 特别关键**：agent 没有跨 session 记忆。没有这两张表，每一个新 session 都会：

- 重新论证已经定过的决策（浪费）
- 或者更糟——**"修复"一个故意的设计**（破坏）

CLAUDE.md 里因此有一条硬约定：

> Before treating something as a bug: check the "decided-not-to-fix" tables — some odd-looking behavior is a documented, deliberate non-fix.

这两张表回答的是"**为什么不这么做**"。这类信息在代码里读不出来，也是 agent 最容易丢失的信息。

### 3.4 把架构约定变成会失败的测试

`internal/archtest` 是全项目最值得抄的一个包。它做两件事：

- **import 边界**：`internal/report` 不得依赖 `router`/`server`/`config`；`ctxgraph` 不得依赖 `report`/`story`
- **文件行数预算**：`router.go` ≤ 700、`aggregate.go` ≤ 1000、`config.go` ≤ 750

**为什么这对非技术背景的人是关键杠杆**：你审不了 25,000 行 Go 代码。但你可以在 agent 说出一条规则时，立刻回一句：**"把这条规则写成一个会失败的测试。"**

从此这条规则不再依赖任何人的记忆或自觉。router.go 长回 948 行的那天，`go test` 会红。

这个模式适用于任何领域，不限于代码架构。规则一旦能被机械检查，它就从"我们说好的"变成了"改不了的"。

### 3.5 评审是消耗品，结论必须落进永久载体

D11–D24 期间至少 20 份评审/分析文档被创建又删除，其中 07-17 一天内出现四份来自不同模型的独立审计。

**这个模式的完整形态是三步**：

1. 让**另一个模型**（不是写代码的那个）做独立评审，明确要求"不以现有文档为准绳，读代码得出结论，再回头看文档是否一致"
2. 逐条处理评审结论
3. **把结论沉淀进三个永久载体之一，然后删掉评审文档**：
   - 设计文档的决策表（"我们考虑过，选了 A，因为……"）
   - 决定不修表（"我们知道，故意不改，因为……"）
   - 一个会失败的测试（"以后不许再犯"）

**落不进这三个地方的评审结论，等于没做完。**

D28 又完整走了一轮这个模式：另一个模型不以设计文档为准绳读代码、逐条给出发现，结论处理完之后，评审文档本身也按惯例删除了。这句话是整套方法的精髓——**让评审者去质疑文档，而不是拿文档当标准答案**。因为在 vibe coding 项目里，文档和代码是同一批 agent 写的，它们会一起错。

### 3.6 敢于推翻自己的设计——而且是在用过之后

至少 7 次实质性反转：

| 原方案 | 反转后 | 间隔 |
| --- | --- | --- |
| 复用官方 Provider SDK（v1 文档明确建议） | 零 SDK | 1 天 |
| Canonical Request/Response 中间表示（v1 文档明确建议） | 字节透传，永不翻译 | 1 天 |
| 字节级状态机（4 个版本） | 全量缓冲 | 1 天 |
| 全量缓冲（正确但假流式） | SSE 事件粒度 | 1 天 |
| providers 按协议分两层 map | 扁平多协议列表 | 23 天 |
| base_url 路径重叠自动消除 | 删掉，要求 base_url 自带版本 | 3 天 |
| `internal/report2`（新包并行开发） | 合并回 `internal/report` | 1 天 |
| 主动 + 被动双探测模式 | 只留后台探测 | 14 天 |

**最重要的观察**：v1 文档里两条最"专业"、最像正确架构建议的条款——用 SDK、定义 IR——**全是错的**。而它们只有在写完代码、真的跑起来之后才被证伪。

这是 MVP-first 的实际含义，也是 vibe coding 相对传统开发的真正优势所在：**当实现的成本降到一天，"先建后拆"就从奢侈变成了默认策略。** 你不需要在纸上把架构想对，你需要的是快速造出来、用一下、然后有勇气推翻。

`report2` 那次尤其典型：新建一个并行包重写报表（`aceb681`），第二天确认新版可用，立刻合并回原包并删掉旧实现（`a709fac` "Merge internal/report2 into internal/report — one package, not two"）。**并行分支只活了一天。**

### 3.7 测试是 agent 的记忆，不是质量保证

测试/生产比从 D1 的 0.66 一路到 0.94，**全程没有一次"欠测试后补"的阶段**。

对传统开发，测试是防回归的。对 vibe coding，它的作用不一样：

**它是唯一能跨 session、跨模型阻止 agent 破坏既有行为的机制。** 你看不懂代码没关系——测试跑不过就是跑不过。设计文档会被误读，注释会被忽略，CLAUDE.md 可能没被加载，但 `go test ./...` 是红的就是红的。

这个项目里的测试形态特别丰富，值得逐类列出（每一类都对应一种"我担心 agent 以后会破坏什么"）：

| 测试类型 | 守的是什么 |
| --- | --- |
| 单元测试（750 个） | 常规行为 |
| Fuzz（2 个） | 手写扫描器在不受信输入下不 panic |
| Golden 文件 | 报表/叙事输出的完整形状（含中英双语） |
| 不变量测试 | tool_call/tool_result 配对必须 100% |
| 一致性测试（4 个） | `report` 的会话分组必须与 `ctxgraph` 一致——**防的是两处实现悄悄分叉** |
| 确定性测试 | 报表在多次运行/平局场景下必须字节一致 |
| 架构测试 | import 边界 + 文件行数预算 |
| e2e | 双语走通完整报表生成 |
| `-race` | 并发正确性 |

### 3.8 给启发式规则配"失效自检信号"

这条最微妙，但对 agent 写的代码特别重要。

vmr 里有一批基于内容特征的启发式规则（识别 MiniMax 的 thinking 泄漏、识别软封禁标记、识别 SSE 换行格式）。它们都可能因为供应商改措辞而**静默失效**。

处理方式不是"写得更全面"，而是：**当字面守卫没触发、但内容形状仍然可疑时，往审计日志打一个标记**（`crlf_framing_suspected`、`thinking_leak_suspected`）。

于是规则失效时，运维侧**会看到信号**，而不是静默降级。

**为什么这对 vibe coding 是刚需**：agent 极擅长写"看起来处理了所有情况"的代码。它会给你一个覆盖 12 种情况的匹配函数，语气笃定。给这类代码配一个"我可能错了"的信号通道，是把 agent 的过度自信转成可观测事实的唯一办法。

同一思路的另一个体现，是设计文档里对没校准过的阈值的诚实标注，以及 Analytics 文档专门有一节叫**"盲区（诚实声明，避免'没记录'被误读成'没发生'）"**。

---

## 4. 负面经验：踩过的坑

以下都是真实付出过的代价，不是假设的风险；每条一句话讲清楚发生过什么、教训是什么。**这里不追踪 vmr 当前是否已经改掉——那是 `docs/OUTSTANDING_ISSUES_opus-5.md` 的职责，本文只谈经验。**

- **交叉引用一旦带编号，迟早悬空**：agent 天然爱写"见 §6.5"这类精确引用，看起来很专业；工作文档又习惯用完即删（3.5 节的好习惯）。两者相乘，开发越快，指向虚空的指针积累得越快，且最新的模块往往受灾最重。**教训**：交叉引用只写文档名 + 章节名或一句话描述，不写编号；删除任何工作文档之前，先 grep 代码里对它的引用。

- **写给 agent 看的项目简报，自己也会漂移，而且会反复漂移**：CLAUDE.md 是每个新 session 的第一入口，一条过时的陈述会被无条件继承——人会怀疑过时的文档，agent 不会怀疑给自己看的简报，代码加了新协议、新 CI job 之后，简报可以在被修正没几天内又漂出下一批陈旧陈述。**教训**：项目简报要定期核对，最好由另一个模型逐条对照代码检查，不能让写代码的那个模型自查；它是配置，不是写一次就完事的文档。

- **产能太高会在不知不觉中换重心，文档不会自动跟上**：分析层的代码量在没人正式决定的情况下超过了路由核心，人自己在对外文案里已经悄悄承认了这次转向，项目简报和设计文档却没有同步。**教训**：定期回看第一天写的定位文档，问自己"现在做的还是当初那个东西吗"；答案是"不是"完全可以接受，但要明确改写定位，而不是让文档和现实各说各话——承认漂移和同步文档是两个动作，不会自动一起发生。

- **跨 session 的接缝没人负责**：一个 session 写了配置一致性检查函数，另一个 session 加了热重载路径，两个产物各自看都对，但从没人把两者接起来，检查函数在热重载路径上因此从未被调用。**教训**："最近新增的能力有没有接到已有的调用路径上"，是人必须定期检查的事，agent 不会主动去查。

- **局部正确的决策，乘起来可能是错的**：几个内存缓冲上限分别定义在不同包里，各自的理由都站得住脚，却从来没有一处文档或测试把它们乘起来看过；更微妙的是，即使乘积被发现了，agent 也未必知道"多大才算合理"——它能算乘积，但不会主动去找一个外部参照系。**教训**：跨模块的资源上界要靠人显式拉出来列成一张表相乘着看；而"该设多大"这一步，往往还需要人带一个具体的外部锚点（比如当下主流模型的上下文窗口量级）进来，agent 才有的算。

- **agent 只测它认为是"代码"的东西**：Go 代码测得很认真（测试/生产比长期 0.9+），但 shell 脚本、CI 配置、配置示例这些"周边"长期是盲区，agent 不会主动想到要测它们。**教训**：这类盲区要靠人显式点名要求；点名之后修起来往往比想象中快——难的不是"要不要修"，是"有没有想到要修"。

- **agent 产出内容的边际成本太低，会导致不加区分地堆积**：市场/分发类文档（知乎回答、Reddit 回复、文章草稿……）和工程文档一度混在同一个目录里，而且这个问题在被记录、被讨论的同时还在继续变严重。**教训**：产出物要按用途分区存放，这条边界只能靠人来画，agent 不会自己判断"这是工程资产"还是"这是营销素材"。

- **commit 记录本身的质量参差不齐**：既有 agent 起草的结构化多行 message，也出现过一整张终端渲染表格直接粘进 commit message 的情况，还有一批短于 14 字符的"update"/"cleanup"。**教训**：commit message 的质量取决于有没有人在 review 环节把关，agent 自己不会对这件事有一致的标准。

**这些负面经验有一个共同形状**：它们全部发生在"局部"与"整体"的接缝上——单条编号引用没问题，几百条一起加速腐化才是问题；单个缓冲上限没问题，三个乘起来才是问题；单个 session 的产物没问题，接缝处没人管才是问题。agent 在局部的质量已经超过多数人类项目的平均水平；接缝处的整体性，仍然是人必须在场的地方。

---

## 5. 迁移到新项目：最小可行流程

面向"换一个技术领域"或"技术背景不强但想做真实软件项目"的场景。

### 5.1 人不可外包的四个动作

其余一切都可以交给 agent。这四件不行：

1. **定边界** —— 尤其是那份"绝不做"的清单。agent 的默认方向是加，只有你能说不。
2. **拍反转** —— 用过之后推翻自己早先的设计。agent 会倾向于在既有方案上打补丁，不会主动说"这个方向从根上就错了"。
3. **审接缝** —— 跨模块、跨 session 的整体性。局部正确 × 局部正确 ≠ 全局正确。
4. **承认漂移** —— 定期比对"我在做的"和"我说要做的"，然后**改写定位**，而不是假装没变。

### 5.2 逐日流程

**Day 0（人独立完成，不用 agent）**

写一页纸，回答 3.1 节那五个问题。不写任何实现细节。这份文档你要能一口气读完。

**Day 0.5**

让 agent 把它扩写成完整设计方案。**你逐条读**——你读的不是技术，是**理由**。每一条"为什么选 A 不选 B"，如果你读不懂，那就是它没写清楚，让它重写到你懂为止。这一步筛掉的是 agent 的"看起来很专业但没想清楚"。

**Day 1**

MVP。硬性要求两条：

- 测试与代码同步产出，不接受"先跑通再补测试"
- 用真实数据/真实依赖做一次端到端验证，把结果写下来（vmr 的 `DEV_PLAN.md` 里每条验收都带实测数字）

**Day 2 起，每个特性**

代码 + 测试 + 设计文档，**同一个 commit**。

**每周一次**

换一个模型做独立评审。评审提示词里必须包含："不要以现有文档为准绳，从代码读出结论，再回头看文档是否一致。"

评审结论逐条处理，然后落进三个载体之一，落完删掉评审文档：

- 设计文档的**决策与取舍表**
- **决定不修表**
- 一个**会失败的测试**

**触发式，不按时间**

- 当你第三次向新 session 解释同一件事 → 写 CLAUDE.md（或等价的项目简报）——vmr 大概在第 8–10 天就出现过这个信号，但简报直到第 20 天才写，晚了几天，不算离谱
- 当 agent 说出一条规则（"这个文件不该超过 500 行"、"A 不能依赖 B"）→ 立刻说："把它写成一个会失败的测试"
- 当设计文档超过 800 行 → 考虑重写或拆分，不要继续追加

### 5.3 三份必须存在的文档

| 文档 | 谁写 | 回答什么 | 何时更新 |
| --- | --- | --- | --- |
| 定位文档（一页） | 人 | 做什么 / **不做什么** | 重心真的变了的时候（并且要明确改写） |
| 设计方案（含两张决策表） | agent，人审 | 怎么做 / **为什么不那么做** | 每个特性 |
| 项目简报（CLAUDE.md） | agent，另一个模型核 | 新 session 的最短上手路径 | 每次结构变化；定期交叉核对 |

**两张表比文档正文更重要**：决策与取舍表（为什么选 A 不选 B）、决定不修表（知道它奇怪，故意不改，理由如下）。

### 5.4 适用边界 — 这套方法在什么条件下成立

必须诚实地说：vmr 的几个特性极大地降低了 vibe coding 的风险，换个项目不一定成立。

**vmr 具备的有利条件**：

- **行为可逐字节验证**——"经 vmr 收到的字节 == 直连收到的字节"是一个可以机械检验的命题。这让正确性有了客观裁判。
- 单用户本地工具，**无用户数据、无数据库迁移、无多人协作、无合规要求**
- **零 UI**——没有设计、交互、可访问性这些难以自动验证的维度
- 4 个依赖，无框架、无生态版本纠缠
- 作者本人就是唯一用户，反馈闭环以小时计

**换成下列情况，同样的方法需要额外护栏**：

| 情况 | 需要补什么 |
| --- | --- |
| 有数据库/数据迁移 | 迁移的可逆性和演练必须是人的职责，不能交给 agent 自证 |
| 有 UI | 需要视觉回归、真人试用；agent 无法自验"好不好用" |
| 多人协作 | CLAUDE.md 类文档从"给 agent 看"变成"给人和 agent 共同看"，准确性要求更高 |
| 有真实用户数据 | 审计日志这类"全量记录"的设计要先过隐私关 |
| 目标不可机械验证 | 优先补一个能自动判定的验收信号，没有它，测试就只是在锁定当前行为而不是锁定正确行为 |

**核心判据**：如果这个项目有一个**能自动判定对错的信号**，vibe coding 的规模上限会很高；如果没有，人必须在每一步都在场，产能优势会大幅缩水。

---

## 6. 一句话总结

这个项目的成功不来自任何一次聪明的 prompt，而来自一套**把知识往仓库里沉淀的纪律**：一页人写的边界文档定住了范围；两张决策表让 27 天里至少 5 个不同模型不必重新论证已经定过的事；测试和一个 archtest 包让"我们说好的"变成了"改不了的"；每周一轮换模型的独立评审，结论落进永久载体后即刻删掉评审本身。

它的负面经验同样真实，而且都是同一个形状：编号交叉引用会悬空、给 agent 看的简报自己会漂移、产能太高会换重心而不自知、跨 session 的接缝没人天然负责、局部正确的决策乘起来可能是错的、agent 不会主动测"周边"、内容产出太便宜会不加区分地堆积。**这些代价全部发生在"局部"与"整体"的接缝上。** agent 在局部的质量已经超过多数人类项目的平均水平；整体性——跨模块、跨 session、跨时间的一致性——仍然是人必须在场的地方。

这大概就是当前阶段 vibe coding 最准确的分工线。

---

## 附录：可直接复用的清单

**项目启动时**

- [ ] 一页定位文档，含明确排除清单，人自己写
- [ ] 让 agent 扩写成设计方案，逐条读"理由"部分
- [ ] MVP 要求测试同步产出 + 真实端到端验证并记录数字

**每个特性**

- [ ] 代码 + 测试 + 设计文档同一个 commit
- [ ] 新决策进"决策与取舍表"（决策 / 备选 / 取舍逻辑）
- [ ] 决定不改的怪异行为进"决定不修表"（项 / 现状 / 不动的理由）

**每周**

- [ ] 换模型做独立评审，要求"从代码读结论，再核对文档"
- [ ] 评审结论落进：设计文档决策表 / 决定不修表 / 一个会失败的测试
- [ ] 落完删掉评审文档，**删之前先 grep 代码里对它的引用**

**触发式**

- [ ] 第三次重复解释同一件事 → 写项目简报
- [ ] agent 说出一条规则 → "把它写成一个会失败的测试"
- [ ] 设计文档 > 800 行 → 重写或拆分
- [ ] 加了新的检查函数 → 确认它被接到了自动路径上

**定期人工检查（agent 不会做）**

- [ ] 所有资源上界列成一张表，相乘看一遍
- [ ] 项目简报的每条声明逐条对照代码（让另一个模型做）
- [ ] 各层代码量对比 vs 定位文档说的重心
- [ ] shell 脚本、配置示例、CI 这些"非代码"部分有没有守护

**从一开始就定死的约定**

- [ ] 注释和文档之间**不用编号交叉引用**，写章节名或一句话描述
- [ ] 启发式规则必须配"失效自检信号"
- [ ] 注释记录"被替换掉的旧方案错在哪"，而不只是"现在这个方案是什么"
