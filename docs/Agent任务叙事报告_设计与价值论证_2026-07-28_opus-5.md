// Ver 2026-07-29 19:20, by Sonnet 5

# Agent 任务叙事报告（`vmr story`）—— 第一性原理推导、价值论证与渐进实现方案

> **v2.0**（v1.0 见附录 B 的修订清单：本轮推翻了 v1 的三处结构性判断，补了两处遗漏）
> 关联：`CCR_特性借鉴分析报告_opus-5.md` §2.4（Context Archive）、§5 N-4（Compaction 感知与告警）
> 证据源：`reports/`（7112 条记录 / 15 天 / 752 会话 / 809K 条消息实例）+ `internal/report/{session,render,aggregate,metrics,rows}.go` + 设计文档 §9
> 方法：所有结论都在真实审计数据上跑过验证脚本，附录 A 给出可复现的实测数字

---

## 0. Debrief

需求是**一个新的产物类别**，不是 `vmr-report.md` 的又一节：把一次 Agent 任务的完整执行过程还原成可读的叙事流，每一步既有压缩后的解读，又能展开原始证据。

三层动机，重要性递增：

1. 看懂单个 Agent 怎么干活——上下文怎么组织、工具怎么用、哪一步开始跑偏；
2. 横向对比不同 Agent 系统（OpenClaw / Lobster 内嵌版 / Hermes / Pi）在同一任务上的行为差异；
3. 把 `docs/openclaw_dual_instance_analysis_2026-07-28_v1.0_deepseek-v4-pro.md` 那种手工分析产品化。

三个技术障碍：任务串联的稳定性、compaction 还原、subagent（本期暂缓，设计留位置）。

已拍板前提：设计文档先行；报告以 Journey 为文件、内部按 task 分章；LLM 复用 `config.yaml` 已有 endpoint 直连上游；通用内核 + agent 特化插件。

---

## 1. 结论先行

**做。但 v1.0 的方案结构是错的，本版给出修正后的最优解和渐进落地路径。**

一句话的第一性原理结论：

> **VMR 手里的不是"日志"，而是同一份对话状态在时间轴上的完整快照序列。所有关于 Agent 行为的问题，本质都是对这个序列做差分。因此正确的底层抽象不是"请求记录 + 启发式分组"，而是内容寻址的消息池 + 每请求一份清单（manifest）+ 清单之间的编辑关系图——这是 git 的模型。会话、任务、compaction、上下文生命周期、subagent，全部退化成这张图上的查询，而不是各自独立的启发式规则。**

而现有实现里，这张图的核心数据结构**已经存在**——`ReqInfo.keys`（每条非 system 消息的内容 hash 向量）——但它被当成 LCP 选父的中间变量，用完即弃。**技术债的确切位置就在这里：最有价值的数据结构被降级成了一个私有字段。**

因此方案是：把 manifest 提升为一等公民（`internal/ctxgraph`），`story` 与 `report` 都成为它的视图；分三阶段渐进偿还，每阶段独立可交付，并用一致性测试防止分叉。

**Effort 约 20.5 人天**（v1.0 估的 14.5 偏低，见 §8 的诚实重估），其中前 11.5 天构成完整闭环、不含任何 LLM 成本，剩下 9 天是四个互不依赖的可选模块。**第一步只要 3.5 天**就能出可读产物——实测 95.86% 的上下文编辑是纯追加（附录 A.7），所以"切分"能独立交付，"缝合"才是贵的那部分。可执行的分步计划见附录 C。

---

## 2. 第一性原理：这个问题的本质是什么

### 2.1 假设只有原始日志，从零开始该怎么做

抛开 VMR 现有代码，只给定"一堆按时间排序的完整请求体"，问：要回答"这个 Agent 是怎么解决这个问题的"，最小充分表示是什么？

推导链：

1. **观测的原子事实**是：`(时刻 t, 完整对话状态 S(t), 上游响应 R(t))`。VMR 的审计记录**恰好就是这个三元组**，一条不多一条不少。
2. **对话状态 S 是一个有序的消息列表**。消息是不可变的值——同样的内容就是同一条消息，无论它出现在第几次请求的第几个位置。因此消息应当**内容寻址**。
3. 于是 `S(t)` 退化为一个**哈希向量**（manifest）：`M(t) = [h₀, h₁, …, hₙ]`。整个语料就是一个 manifest 的时间序列。
4. Agent 每轮重发完整历史，所以相邻 manifest 高度相似。**Agent 对自己历史做的每一个动作，都表现为 `M(t) → M(t+1)` 的一次编辑。**
5. 因此：**理解 Agent = 对 manifest 序列做差分并给每次编辑分类。**

这就是 git：blob（消息）+ tree/manifest（对话状态）+ commit 图（编辑关系）。这个类比不是修辞，它直接给出了实现。

### 2.2 编辑类型学：五种编辑，覆盖全部现象

给定同一 lineage 上相邻的 `M(t)` 与 `M(t+1)`，用 LCP + 哈希集合运算即可分类（全部 O(n)）：

| 编辑 | 判据 | 语义 |
| --- | --- | --- |
| `append` | `M(t)` 是 `M(t+1)` 的前缀 | 正常推进：新的 assistant / tool_result 追加 |
| `replace_tail` | 有公共前缀 p，尾部发散，长度相近 | 临时尾替换、重试、ephemeral message |
| `splice` | `M(t+1) = M(t)[:k] + [新 blob] + M(t)[j:]` | **原地替换**：中段被改写（见 F11 的 S2 型 compaction） |
| `contract` | `\|M(t+1)\| ≪ \|M(t)\|` 且内容基本是 `M(t)` 的子集 | 截断 / 重置 / 外挂式 compaction 后的重建 |
| `fork` | 重叠率低于阈值 | 新 lineage 起点 |

**关键推论：compaction 不该靠识别模板来检测，而该靠检测"非追加型编辑"来检测。** 模板只是事后分类的补充证据。这一条让整套机制天然框架无关——不认识 OpenClaw 的模板也照样能发现"这里历史被动过手术了"。v1.0 把这个因果关系搞反了（见附录 B 修订 2）。

### 2.3 所有概念都是这张图上的查询

| 概念 | 在图上的定义 |
| --- | --- |
| Lineage（连续片段） | 由 `append` / `replace_tail` 边连成的链 |
| Journey（逻辑任务） | 跨 `splice` / `contract` / `fork` 缝合后的 lineage 路径 |
| Task | Journey 上"新增真实用户指令"事件切出的区间 |
| Event | 一个 blob 在该 Journey 上的首次出现 |
| 上下文生命周期 | 一个 blob 出现在哪些 manifest 里 → 首见 step、末见 step、消失原因 |
| Compaction | 一次 `splice` 或 `contract` 编辑 + （若有）对应的 summarization 请求 |
| 信息损失 | 被编辑掉的 blob 集合 vs 新 blob 的 token 差 + 实体覆盖率 |
| Subagent | 另一个 lineage，其根 manifest 与主 lineage 共享 system blob，且其输出 blob 出现在主 lineage 后续某个 tool_result blob 里 |

**七个原本各自需要一套启发式的问题，坍缩成同一张图上的七个查询。** 这是判断这个抽象是否正确的标准，也是它相对 v1.0（每个问题一套规则）的根本优势。

### 2.4 为什么 VMR 不需要数据库，而 CCR 需要

CCR 的 Context Archive 用了 SQLite + `generation` 版本链 + `parent_archive_id` + 保留策略，`context-archive.ts` 840 行 + `store.ts` + `protocol.ts` 798 行。

差别不在工程能力，在**时机**：

- **CCR 在写入时归档** → 必须有一个独立的存储层，于是需要数据库、版本链、保留策略、权限管理；
- **VMR 在读取时重建** → 审计日志本身就是内容寻址存储（全量原始 body 已经在盘上，且不可变、按时间有序），只需要一个**索引**。

所以对 VMR 来说，"归档"是冗余的——它早就无损保存了全部内容，只是没建索引。这直接兑现了 CCR 报告里 N-4 的判断（"VMR 不需要也不应该去修复 compaction，但它可以成为唯一能告诉你 compaction 发生了什么的工具"），而且比 N-4 设想的观测版更进一步：不只是告警，是**完整还原**。

**实测可行性**（附录 A.5）：7112 条记录 / 809K 条消息实例。索引只存 hash + 位置，不存内容：

- manifest 总计 809K × 16B ≈ **13MB**
- blob 位置索引（去重后约 10–20 万条）× 48B ≈ **10MB**

内容按需从审计文件回捞。**zstd 不可随机寻址**，所以回捞必须是"按文件批量顺序扫描"——这恰好就是 `Build` 现在的两遍读取模式，不需要新机制。

### 2.5 技术债的确切位置

`session.go:403` 已经在算 manifest：

```go
b, _ := json.Marshal(raw)
r.keys = append(r.keys, fmt.Sprintf("%x", md5.Sum(b)))
```

这个 `keys` 字段是**非导出的、用完即弃的中间变量**，唯一用途是 `attach()` 里的 `lcp()` 选父。于是：

- 想知道"这条消息什么时候消失的" → 没有索引，做不到；
- 想知道"这次 compaction 吞掉了什么" → 只能拿 200 字节文本 needle 去猜（于是有了那 8 条告警）；
- 想知道"session 中途是不是被重置过" → `context_growth = last/first tokens_in` 得到 ×179.5 这种脏值（§7 自己都标了"中途 compaction"）；
- 想做 story → 只能重头再写一套。

**同一个数据结构，被四个功能各自绕过。** 这就是要还的债。

---

## 3. 应用场景 review：它到底用在哪

### 3.1 四个真实场景

| 场景 | 提问 | 需要哪一层 |
| --- | --- | --- |
| **S-A 单 Agent 调优** | 我的 prompt/工具集改了之后，Agent 的行为变了吗？ | 剖面层（指标对比）为主 |
| **S-B 跨框架对比** | Lobster 版比 Standalone 版好在哪？ | 剖面层为主 + 解读层点睛 |
| **S-C 事故复盘** | 这次任务为什么跑偏/为什么花了 3 倍时间？ | 事实层（事件流）+ 解读层 |
| **S-D 上下文工程** | 我的上下文里有多少是浪费的？哪些内容进来之后从没被用到？ | 生命周期视图（事实层） |

S-D 是 v1.0 完全没提的场景，但它可能是**最实用**的一个：上下文生命周期直接回答"哪条 tool_result 进来 5 万 token，之后 40 轮再没被引用，最后被压缩吞掉"——这是可执行的优化建议，不是观察。

### 3.2 从那份 deepseek 报告反推：证据其实来自规则层

这是本轮 review 最重要的自我修正。逐条拆 `openclaw_dual_instance_analysis` 的证据构成：

| 该报告的论据 | 来源 |
| --- | --- |
| 58 轮 vs 22 轮、16 分 vs 7.5 分 | 规则可得 |
| 124 msgs vs 57 msgs、末轮 token 按角色分布 | 规则可得（`roleTokens` 已实现） |
| 缓存命中率逐轮曲线 18%→97% vs 82%→99% | 规则可得（已在 §1 实现） |
| system prompt 20.5K vs 17.1K、工具 67 vs 46 | 规则可得（`ToolShapes` 已实现） |
| 工具调用分布、迭代阶段划分 | 规则可得 |
| "MEMORY.md 是单一最大根因" | **人做的推断**，不是 LLM 做的 |

**结论：那份报告 90% 的证据是规则可得的表格，LLM 在其中承担的是文字组织，而不是发现。** 而它最有价值的那个结论（MEMORY.md 是根因）是人在看到表格差异后推断出来的。

由此推翻 v1.0 的一个结构性判断：**LLM 解读层不是核心价值，规则派生的"行为剖面"才是。** 而剖面层是免费的、确定性的、可复现的、跨框架可比的。它必须进 Phase A，不能像 v1.0 那样根本没出现。

同时这也解释了为什么 P4（双 Journey 对比）可以大幅提前、大幅降本——两份剖面做差就是对比报告的骨架。

### 3.3 因此产物是三层，不是两层

```
① 事实层（规则，零成本，确定性）
   事件流 + 上下文生命周期 + 时间三分解 + compaction 还原与信息损失
② 剖面层（规则派生指标，零成本，跨框架可比）        ← v1.0 遗漏
   工具分布 / 重复动作率 / 错误恢复次数 / 计划-执行比 / 上下文构成演化 / 净工作时长
③ 解读层（LLM，可选，可降级）
   每 Step 的意图-动作-结果注解 + 全局总述
```

**层间约束**：② 只能由 ① 派生，③ 只能引用 ①②，且不得生成任何数字。缺 ③ 时报告完整可用，只是没有散文。

---

## 4. 实测核查

以下每条都在真实数据上验证过，脚本见附录 A。F1–F8 来自 v1.0（F6 有重要补充），F9–F12 是本轮新增。

### F1 — 只读末轮会丢掉 26%–99% 的内容

6 个真实会话，"全部请求去重合并"与"末轮消息集合"之差：

| 会话 | 请求数 | 去重消息 | 末轮消息 | 丢失字符占比 |
| --- | ---: | ---: | ---: | ---: |
| s231（OpenClaw，24 轮） | 24 | 86 | 8 | **81.4%** |
| s290（Pi，跨 5 天） | 444 | 1034 | 5 | **99.2%** |
| s187 | 254 | 562 | 394 | **49.3%** |
| s407 | 342 | 743 | 682 | **26.1%** |
| s634 | 251 | 483 | 222 | **80.8%** |
| s217 | 366 | 1303 | 449 | **81.7%** |

"没有压缩时末轮就是完整上下文"这个假设在真实数据上是错的。

### F2 — Compaction 缝合用消息 hash，不用文本 needle

现状：`linkCompactions()` 拿 session 首条消息前 200 字节，去 substring 匹配 compaction 请求里那段**渲染过的** transcript。失败根因：OpenClaw 首条消息带 `[Thu … GMT+8] [message_id: om_x1…]` 前缀 + envelope JSON，transcript 里渲染成 `[User]: 就你那个skill…`，永远对不上；`stripBracketPrefix` 只剥第一个方括号。

实测（`20260716-155251.355`，60 条消息，#1 为 summary，取 `[2:]` 的 58 条 hash 与当日更早 581 条请求求交）：

```
match 42/58  20260716-152238.775   <- s231，唯一强峰
match 40/58  20260716-152233.327
match 38/58  20260716-152229.810
match 36/58  20260716-152226.699   <- 同会话更早轮次，单调衰减
```

排序干净、峰值明确、零文本启发式，与现有文本 needle 得到的 `s238 ← s231` 结论一致。

### F3 — Compaction 请求本身不是可靠历史来源，只是锚点

`20260716-155115.506`：body 仅 17.5KB / 6227 token，输入是一段被大幅截断的渲染 transcript（工具结果基本砍光），而它压缩的历史实际有几十 K token。原始历史必须回到 pre-compaction 的最后一轮请求取。

### F4 — "successor 找不到"有时是对的

那条 compaction 的输出（`## Goal\nExtract an **open, unconstrained task brief**…`）在全部 7112 条记录里**只出现在它自己那一条**——结果被丢弃了。缝合逻辑必须区分**已缝合 / 确认无后继 / 匹配失败**三态，不能把后两者混成一条日志。

### F5 — 模板标记必须锚定位置和角色

实测反例：`20260716-031913.090` 命中 `The conversation history before this point was compacted`，但它在**第 120 条 tool 消息**里——Pi agent 正在读 VMR 自己的 `session.go` 源码。规则必须收紧为"首条非 system 消息 + role=user + 位于文本开头"。

### F6 — session 粒度 ≠ 任务，且 anchor 会把两段 lineage 粘在一起

752 个 session 里 `s290` = 444 请求 / 32 任务 / 跨 5 天，末轮只剩 5 条消息。

**本轮新增的更严重发现**：`s231` 内部有一次隐藏的硬断裂。逐轮 manifest 演化（附录 A.3）：

```
turn 1-20:  msgs 3 → 79，LCP 完美跟随，cover 0.60 → 0.97   （纯 append）
turn 21:    msgs 79 → 4，LCP=0，cover=0.25                  <== 断裂
turn 22-24: 重新增长
```

也就是说 `s231` 是**两段 lineage 被同一个 anchor 粘在一起**（第一条非 system 消息——原始用户指令——被 compaction 保留了下来，所以 `SessKey` 不变）。

**所以缝合不只要合并，还要会切分。** v1.0 只设计了合并（journey 缝合），漏了切分。这是 anchor 分组的固有缺陷：它对"历史被换掉但开头没换"完全失明。manifest 编辑类型学天然覆盖这一点——那是一次 `contract` 编辑。

### F7 — 架构硬约束

`archtest` 禁止 `internal/report` 依赖 `router` / `server` / `config`；`render_doc.go` 400 行、`aggregate.go` 1000 行有预算，"新增一节 = 新增一个文件"。本功能不能进 `report` 包。

### F8 — 非 OpenClaw 的 compaction 形态尚未采样

`compaction` 工作负载只有 9 条，形态全是 OpenClaw 的外挂式。Pi 的重置机制不同（F6/F11）；Anthropic 入口仅 59 条请求，Claude Code 的 `/compact` 无法验证。

### F9（新）— 因果边由协议给出，不需要推断

实测 `20260716-152238.775`（79 条消息）：**57 个 `tool_calls` ↔ 57 个 `tool_call_id`，零孤儿。**

这意味着一个 Step 内部的因果结构（哪个 tool_call 产生了哪个 tool_result）是**协议给定的精确事实**，不是启发式。Anthropic 侧同理（`tool_use` / `tool_use_id`，`render.go:156-165` 已经在解析）。

推论：叙事流里"Agent 做了什么 → 得到了什么"这条最核心的边是**零误差**的。需要启发式的只有跨 Step 的 lineage 关系。这大幅收窄了不确定性的作用域。

### F10（新）— 墙钟时间可三分解，且这是 VMR 独有的度量

`ts(N+1) − (ts(N) + dur_ms(N))` 就是 Agent 侧的间隙：工具执行 + 客户端处理 + 人类思考。实测：

| 会话 | 轮次 | 墙钟 | 模型时间 | Agent 侧间隙 | 间隙 p50 / p95 / max |
| --- | ---: | ---: | ---: | ---: | --- |
| s231 | 24 | 2069.9s | 381.0s (18.4%) | 88.0s (4.3%) | 4.2s / 10.2s / 10.4s |
| s187 | 254 | 56665.8s | 5090.4s (9.0%) | 783.4s (1.4%) | 0.2s / 4.0s / 296.0s |
| s634 | 251 | 74720.5s | 4031.2s (5.4%) | 822.7s (1.1%) | 0.2s / 7.1s / 348.8s |
| s407 | 342 | 15365.6s | 3074.9s (20.0%) | 4172.8s (27.2%) | 0.6s / 30.0s / 586.0s |

两个结论：

1. **"墙钟时长"作为效率指标几乎没有意义**——s187 有 90% 的墙钟是人不在场的空等。那份 deepseek 报告的"16 分钟 vs 7.5 分钟"其实混淆了三种时间。
2. **正确的指标是"净工作时长" = 模型时间 + Agent 侧执行时间**，而区分"Agent 侧执行"与"人类空闲"不该用魔法阈值，而该用**间隙之后发生了什么**来分类：紧跟着一条新的真实用户指令 → 人类空闲；紧跟着纯工具循环延续 → Agent 侧执行。这个判据是规则性的，且 `deltaHasNewInstruction()` 已经实现了它需要的判断。

这是一个**免费、新颖、跨框架可比**的指标，v1.0 完全遗漏。

### F11（新）— Compaction 至少有三种形态，v1.0 只建模了一种

| 型 | 机制 | VMR 可见性 | manifest 编辑 |
| --- | --- | --- | --- |
| **S1 外挂式** | 独立的 summarization 请求，之后新 lineage 带 `The conversation history…` 标记 | 完全可见（请求 + 标记 + 尾部） | `contract` + `fork` |
| **S2 原地替换** | 同一 lineage，中段被改写的消息取代，开头保留 | 只在 manifest 差分里可见 | `splice` |
| **S3 静默截断** | 无 LLM 调用，消息直接消失 | 只在 manifest 差分里可见 | `contract` |

S2 的实证（附录 A.4，`s231` turn 20 → 21）：

```
turn 20 (79 msgs): #0 system h=0b0a6cd7ee28 (36579 字符)
                   #1 user   h=6eb2c49e49ea  ← 原始指令
                   #2 assist h=3d6042de2c0e  (120 字符)
turn 21 ( 4 msgs): #0 system h=8c78dc78bcb0 (34060 字符)   ← system prompt 也变了
                   #1 user   h=6eb2c49e49ea  ← 同一条，逐字未变
                   #2 assist h=ed195ccf9337  (2153 字符)   ← 被改写，吸收了摘要
```

中间 76 条工具调用/结果被一条改写后的 assistant 消息取代。

**这引出一个 v1.0 遗漏的建模需求：`revision` 关系。** 在全局 seen-hash 去重下，`ed195ccf9337` 会被当作一个**全新事件**，而 `3d6042de2c0e` 留在流里——渲染出来就像 Agent 把同一件事说了两遍。必须显式标注"B 是 A 在同位置的修订版"（判据：同一 manifest 位置 + 编辑前后共享该位置之前的前缀）。位置信息只用于建立 `revision` 关系，不用于事件流排序。

**同时注意 system prompt 也变了**（36579 → 34060 字符）。system 变更是一个独立的、有分析价值的事件类型（换模型、换工具集、平台注入变化），必须进事件流。

### F12（新）— 语料规模与索引可行性

7112 条记录、809K 条消息实例、平均 113 条/请求；审计源文件 zstd 后 124MB（明文 GB 级）。索引化后 manifest ≈ 13MB、blob 位置索引 ≈ 10MB，全内存无压力。内容按需回捞，因 zstd 不可寻址，必须按文件批量顺序扫描——与现有两遍读取模式一致。

---

## 5. 值不值得做

### 5.1 支持的理由

1. **位置独有。** 跨框架（四套 Agent 系统走同一个入口）+ 全量原始 body + **快照时间序列**。第三条是真护城河：Agent 自己不记录它对历史做过的手术，因为对它而言那只是一次内存赋值。
2. **实测证明捷径走不通。** F1（末轮丢 26–99%）、F6（anchor 粘连）、F11（三种 compaction 形态）都说明：任何"取末轮 / 按 session 分组 / 认模板"的简化方案都会给出错误结论。而正确做法的数据已经在盘上了。
3. **免费的那部分价值最大。** §3.2 证明了对比分析的证据 90% 来自规则层。剖面层零成本、确定性、可复现。
4. **顺手偿还既有技术债。** Phase B 让 `report` 自身受益：修掉 8 条 compaction linking 告警、修掉 `context_growth` 的 ×179.5 脏值、补上 N-4 章节。这不是为 story 做的牺牲，是 report 自己欠的。

### 5.2 反对的理由与缓解

| 风险 | 说明 | 缓解 |
| --- | --- | --- |
| **受众窄** | 直接用户是本团队 + 少数评测者 | 承认它是定位型而非增长型功能；Phase A 零 LLM 成本，先证明自己有用 |
| **LLM 引入不确定性** | 不可复现、有成本、有幻觉 | 三层强制分层（§3.3）；默认不开；注解按 `(step_hash, model, prompt_version)` 落盘缓存，重跑稳定 |
| **启发式债务** | 无 ground truth 可验证缝合对错 | 内核只用协议通用信号；F9 已证明 Step 内因果是零误差的，不确定性只作用在跨 Step 的 lineage 关系上；每条边带证据 + 置信度，宁断勿错 |
| **范围扩张** | VMR 迄今最大的一次扩张 | 独立包 + 独立命令 + archtest 边界；随时可整体砍掉而不影响路由 |
| **隐私** | 产物含完整对话正文 | 沿用 `details/` 的 0600/0700；且见 §5.4 的诚实澄清 |
| **重构风险** | Phase B 动 `report` 的核心分组 | 一致性测试先行（§7.2），Phase B 是机械迁移而非重写 |

### 5.3 与既有原则的冲突复核

设计文档 §9 写死"只记录不分析"。处理方式就是 §3.3 的三层分层：

- **事实层 / 剖面层**：纯规则，与现有 report 同性质，**不构成原则突破**；
- **解读层**：LLM 只产出自然语言，必须携带引用的 step id，渲染为可折叠的独立样式块，旁边永远有原始证据入口；不配置分析端点时整层消失，报告仍完整可用（与 `pricing.yaml` 缺失时 §2 优雅降级同一套路）。

**LLM 说的话不是证据，只是索引。** 在这个约束下黑匣子定位不破。

### 5.4 关于"传播价值"的诚实澄清（v1.0 的一处夸大）

v1.0 说"一张 Agent 执行瀑布图是天然的内容素材"。这话本身没错，但 v1.0 同时把脱敏列为"延后到有需求再做"——**这两条是矛盾的**：任何真实 journey 都含完整对话正文、文件路径、内部项目名。

要么放弃传播这条价值主张，要么把脱敏提升为它的前置条件。建议后者：在 Phase D（HTML）里同时做 `--redact` 模式（保留结构与指标，正文替换为长度占位符 + 类型标签）。在此之前，传播价值记为 **0**，不计入 ROI。

### 5.5 成本实测估算

按 `pricing.yaml` 现价（最低 USD0.14/1M in ≈ ¥1/1M；MiniMax ¥1.20/1M in、¥8/1M out；汇总用 deepseek-v4-pro ≈ USD0.435/1M in）：

| 场景 | 去重内容量 | 解读输入（含 1.5× 滑窗） | 估算成本 |
| --- | ---: | ---: | ---: |
| 典型单任务（s231，24 轮） | ~80K token | ~120K | **¥0.15–0.25** |
| 中型（s187，254 请求） | ~550K token | ~800K | **¥1.0–1.3** |
| 极端（s290，444 请求 / 5 天） | ~1M token | ~1.5M | **¥2.0–2.5** |

不构成障碍，前提是两条硬约束：**按需触发**（绝不在 `vmr report` 里全量跑——752 个会话是几百块且 99% 没人看）、**落盘缓存**。再加一条操作约束：跑前 dry-run 打印预估 token 与金额并要求确认。

注意：Phase A/B 交付的事实层 + 剖面层**成本为零**。LLM 只在 Phase C 才出现，而彼时是否还需要它，应该用 Phase A/B 的实际使用体验来决定。

---

## 6. 方案设计（第一性原理版）

### 6.1 概念模型

```
ctxgraph（内容寻址层）
  Blob      一条消息的内容 hash → (path, line, msgIndex) 位置索引，内容不驻留
  Manifest  一次请求的 hash 向量 + system blob + 元数据（ts, dur, endpoint, usage…）
  Edit      相邻 manifest 的差分分类（append / replace_tail / splice / contract / fork）
  Lineage   由 append / replace_tail 连成的链
  Graph     lineage 之间由 splice / contract / fork 边连接，每条边带 kind + evidence + confidence

story（视图层）
  Journey ── Task ── Step ── Event
                              └ revision 关系（F11）
```

Event 类型：`user_message` / `assistant_text` / `thinking` / `tool_call` / `tool_result` / `compaction_summary` / `system_change`。
每个 Event 带生命周期：首见 step、末见 step、消失原因（`truncated` / `pruned` / `compacted` / `reset` / `alive`）。

### 6.2 Journey 构建：切分 + 合并

**先切分**（F6 的教训）：在同一 SessKey 内，遇到 `contract` / `fork` 编辑就断开成独立 lineage。不能假设 anchor 相同就是同一段。

**再合并**：对每个 lineage 的首 manifest `B₀`，用 blob 倒排索引（hash → lineage）找覆盖率最高的前驱 `A`，`score = |H(B₀) ∩ H(A)| / |H(B₀)|`。边分四类：

| kind | 触发条件 | 置信度 |
| --- | --- | --- |
| `splice` | 同 lineage 内的中段替换（F11 的 S2） | 高（结构判定） |
| `compaction` | `contract` 编辑 + 尾部 hash 强覆盖 +（可选）标记/summarization 请求 | 高 |
| `head_prune` | 头部剪枝，后续 hash 大量重叠 | 中 |
| `same_chat` | 仅 chat_id 相同 + 时间邻近，无内容重叠 | 低——**默认不缝合**，只标注"疑似同源" |

**原则：宁可断开，不要错连。** 低置信度的边渲染成显式的「⟂ 此处可能断裂（未找到足够证据）」。这是整个功能可信度的唯一来源。

### 6.3 事件流与生命周期

全局 seen-hash 去重，按（请求时序，消息位置）首次出现排序。同时维护每个 blob 的出现区间，journey 结束时回填消失原因。位置信息**只**用于建立 `revision` 关系，不参与排序。

### 6.4 Compaction 还原与信息损失（= CCR N-4 的落地）

compaction 渲染为特殊 Step，三块内容：

1. **被吞掉的原始 events 全量**（默认折叠，从 blob 索引回捞）——这是相对 Agent 自身日志的核心优势；
2. **summary 原文**（S1 有独立请求，S2 是被改写的消息）；
3. **信息损失度量**：压缩前后 token 对比 + 规则粗筛的实体覆盖率（被吞段里出现过的文件路径 / 工具名 / URL，有多少在 summary 中彻底消失）。

纯规则、零 LLM 成本、可复现。**不修复，只揭示**——CCR 花 1600+ 行修一个它自己也修不好的问题，VMR 只需如实呈现"这次压缩把 42K token 压成 1.9K，其中这 7 个文件路径和 3 次工具调用在摘要里消失了"。

### 6.5 行为剖面（剖面层，v1.0 遗漏）

全部规则派生、跨框架可比：

| 指标 | 定义 | 回答什么 |
| --- | --- | --- |
| 净工作时长 | 模型时间 + Agent 侧执行时间（F10 的间隙分类） | 真实效率，剔除人类空闲 |
| 模型/工具时间比 | 同上两项之比 | 瓶颈在推理还是在执行 |
| 工具调用分布 | 按工具名的次数与 token 占比 | 工作方式画像 |
| 重复动作率 | 相同 tool_call 参数 hash 的重复次数 | 是否在原地打转 |
| 错误恢复次数 | `is_error` / 非零退出的 tool_result 后的重试 | 韧性 |
| 计划-执行比 | 无 tool_call 的纯文本轮次 / 总轮次 | 思考与行动的配比 |
| 上下文构成演化 | 每轮 system/user/assistant/tool token 占比曲线 | 上下文预算流向 |
| 上下文有效率 | 被后续引用过的 blob token / 总 blob token | 上下文浪费程度（S-D 场景） |
| compaction 次数与损失 | §6.4 | 信息断层 |

两份剖面做差 = 对比报告的骨架（Phase F 因此大幅降本）。

### 6.6 渲染

- **Phase A Markdown**：`{out}/stories/journey-<id>.md`，0600/0700，原始内容 `<details>` 折叠，与现有 detail 页风格一致。**`<id>` 的实际格式与 Step 的实际渲染骨架已在附录 C.3/F.2/F.4 更新（2026-07-29），以附录 C.3 里的"实际骨架"为准，不要照抄本节写作时的旧例子。**
- **Phase D HTML**：单文件自包含（内联 CSS/JS，零外部依赖）。左侧时间轴 + 右侧 Step 卡片瀑布；卡片正面是骨干描述，点开是原始 body。同时实现 `--redact`（§5.4）。

---

## 7. 回到 VMR 现实：落地路径与技术债偿还

### 7.1 第一性原理最优解与现状的差距

| 维度 | 最优解 | VMR 现状 | 差距性质 |
| --- | --- | --- | --- |
| manifest | 一等公民，带索引 | `ReqInfo.keys` 私有中间变量 | **提升可见性**，逻辑已存在 |
| blob 位置索引 | 有 | 无 | 新增，但只是 collect 时多存三个 int |
| 编辑分类 | 五类显式建模 | 只有 `lcp()` + `ReplacedTail` 计数 | 新增，O(n) 纯函数 |
| lineage 切分 | 按编辑类型切 | 按 anchor 分组，会粘连（F6） | **现有行为有缺陷**，需修正 |
| compaction 链接 | hash 集合匹配 | 200 字节文本 needle | **现有行为有缺陷**，需修正 |
| 消息/SSE 解析 | 共享 | `report/render.go` 私有 | 下沉即可，纯函数有测试 |

**结论：差距不大，而且大部分是"现有实现本来就该修的缺陷"，不是"为 story 额外付的代价"。** 这一点决定了 ROI 判断——第一性原理方案在这里不是奢侈品。

### 7.2 渐进计划的结构（可执行版见附录 C）

> 本节讲**为什么这么分阶段**；**开工用的任务清单、验收标准和不做清单在附录 C**。
> 阶段映射：本节的 Phase A 在附录 C 里按"能否缝合"拆成了第 1 步（切分，3.5 天）与第 2 步（缝合 + 剖面，5 天）——因为实测发现 95.86% 的边是纯 append（附录 A.7），切分能独立交付而缝合不能。Phase B = 第 3 步；Phase C/D/E/F = 第 4 步的四个可选模块。


**Phase A — 建立 `internal/ctxgraph`，`story` 跑起来（`report` 不动）**

- 新包实现 blob 索引 / manifest / 编辑分类 / lineage 切分与缝合；
- `story` 在其上做事实层 + 剖面层 + Markdown 渲染；
- `report` 完全不改，零回归风险。

**防分叉机制（关键）**：Phase A 必须同时交付一个**一致性测试**——用 `ctxgraph` 重建的 session 分组，与 `report.AnalyzeSessions` 在同一语料上的输出做对比，差异必须**逐条被显式声明**（要么"应当一致"，要么"这是 ctxgraph 修正了 report 的已知缺陷，见 F6/F2"）。

没有这个测试，Phase A 就会变成一个永久分叉，Phase B 永远不会发生。有了它，Phase B 变成机械迁移。

**Phase B — `report` 迁移到 `ctxgraph`，偿还技术债**

- `session.go` 的分组改为消费 `ctxgraph` 的 lineage，删掉私有 `keys`；
- 顺带修掉：8 条 compaction linking 告警、`context_growth` 的 ×179.5 脏值、F6 的 lineage 粘连；
- 新增 `report` 的 compaction 章节（= CCR N-4），作为 `section_compaction.go`（符合"新增一节 = 新增一个文件"的预算约束）；
- `archtest` 增加 `ctxgraph` 的边界规则。

**Phase C 及之后** — LLM 解读层 / HTML / subagent / 对比报告。

**顺序建议**：`A → B → C`。理由是趁 ctxgraph 设计还新鲜时把 `report` 迁过去，且 Phase B 有独立的用户价值（修 report 自身的缺陷）。

**可接受的替代**：`A → C → B`，**当且仅当** Phase A 的一致性测试已经落地。C 不触碰 `report`，所以推迟 B 不产生新债；一致性测试是防止分叉calcify 的唯一保险。

**明确的终局状态**：一套 lineage 模型，两个渲染器（运维报告 + 叙事报告）。这是终点，不是妥协；上面三个阶段是逼近它的路径。

### 7.3 包与命令边界

```
internal/chatmsg   ← 新：从 report/render.go 下沉的纯函数
                     chatMessage / chatMessages / renderContent / reassembleSSE /
                     finalMessage / toolNames（无状态、已有测试，搬迁风险低）
internal/ctxgraph  ← 新：blob 索引 + manifest + 编辑分类 + lineage 图
                     依赖 {audit, core, chatmsg}
internal/story     ← 新：journey 视图 + 事实层 + 剖面层 + 渲染 + LLM 客户端
                     依赖 {ctxgraph, chatmsg, core, config}
internal/report    ← Phase B 起依赖 {ctxgraph, chatmsg, audit, core}
cmd/vmr/cmd_story.go
```

`archtest` 新增规则：`ctxgraph` 不得依赖 `router` / `server` / `config` / `report` / `story`；`story` 不得依赖 `router` / `server` / `report`。

**这里推翻了 v1.0 的一个结构性判断。** v1.0 说"`story` 不依赖 `report`"——方向对（不该让 report 为 story 开导出口子），但**解耦的层选错了**：正确答案不是两边各写一套，而是**两边共同依赖一个更低的层**。v1.0 那个方案会造成 lineage 逻辑的永久二重实现，正是要避免的东西。

CLI：`vmr story [--session sNN | --journey jNN | --chat <id> | --since/--until] [--narrate] [--redact] [--dry-run] [-o dir] <file|glob>...`。`vmr.sh` 的 `passthrough` 自动转发，脚本无需改动。

（命名备注：Phase A 的产物是事实 + 指标，"story" 听起来像散文。考虑过 `vmr journey` / `vmr trace`，但 `--narrate` 已经把可选的散文层标出来了，且 `story` 更贴合"一个任务的来龙去脉"这个用户心智。保留 `story`。）

---

## 8. 分期与 effort（诚实重估）

> 本节是**总量与口径**；实际排期以**附录 C.2 的四步计划**为准（同一批工作、同一个总量 20.5 天，只是把 Phase A 按可交付性拆成了两步）。


| 阶段 | 内容 | 人天 | 独立可交付 |
| --- | --- | ---: | --- |
| **A** | `chatmsg` 下沉 0.5 + `ctxgraph`（blob 索引/manifest/编辑分类/lineage 切分与缝合）2.5 + `story` 事实层 1.5 + 剖面层 1 + Markdown 渲染 1 + CLI 0.5 + **一致性测试 1** + 拆 `splice/tail` 混合桶 0.5 | **8.5** | 是（附录 C 里拆成第 1、2 步，各自独立可交付） |
| **B** | `report` 迁移到 `ctxgraph` + 修 compaction linking / context_growth / lineage 粘连 + `section_compaction.go` + archtest | **3** | 是：`vmr report` 自身缺陷修复 + N-4 章节 |
| **C** | LLM 解读层：prompt、结构化输出、落盘缓存、dry-run 预算、失败降级 | **2.5** | 是 |
| **D** | 单文件自包含 HTML 瀑布流 + `--redact` | **2.5** | 是 |
| **E** | Subagent 树（含先期采样验证 §10.1 的三条信号） | **2.5** | 是 |
| **F** | 双 Journey 对比报告（剖面做差 + 差异归因） | **1.5** | 是 |
| | **合计** | **20.5** | |

**与 v1.0（14.5）的差异说明**：+6 天。其中 +3 是 `ctxgraph`、一致性测试与编辑分类细化（买的是"不用推倒重来"），+3 是 Phase B（买的是 report 自身缺陷修复 + 债务出清），+0.5 是剖面层与 `--redact`；−2 是 Phase F 因剖面层前置而降本。

**v1.0 的 14.5 天是低估**，因为它把 lineage 逻辑当成 story 私有的一次性工作，没算 report 迟早要重做同一件事的成本。

---

## 9. 风险登记

| ID | 风险 | 概率 | 影响 | 缓解 |
| --- | --- | --- | --- | --- |
| R1 | 缝合误判，把两个不相关任务缝成一个 journey | 中 | 高 | 保守阈值 + 边带置信度 + 显式渲染断裂点；F9 已证明 Step 内因果零误差，不确定性只在跨 Step 边 |
| R2 | **lineage 误切分**（把一个任务切成两段） | 中 | 中 | 与 R1 对称的失败模式，v1.0 未识别。切分阈值同样保守，切分点必须渲染出来并给出证据 |
| R3 | LLM 幻觉污染结论 | 中 | 高 | 三层分层；数字只由规则产生；注解带 step 引用 |
| R4 | 成本失控 | 低 | 中 | dry-run 预估 + 显式确认 + 落盘缓存 + 绝不在 report 里全量跑 |
| R5 | 启发式债务膨胀 | 中 | 中 | 内核只用协议通用信号；agent 特化收敛到 profile 层 |
| R6 | 非 OpenClaw 的 compaction 形态未知（F8） | 高 | 中 | 编辑分类是结构性的，不依赖模板；未识别的编辑标为低置信度而非静默合并 |
| R7 | 隐私 / 传播的矛盾 | — | 中 | §5.4：脱敏是传播价值的前置条件，Phase D 交付前传播价值记 0 |
| R8 | **Phase A 永久分叉，Phase B 不发生** | 中 | 高 | 一致性测试（§7.2）是唯一保险，必须与 Phase A 同批交付，不可延后 |
| R9 | Phase B 动 `report` 核心分组导致回归 | 中 | 中 | 一致性测试先行，迁移变机械操作；`session_test.go` 534 行现有覆盖作为安全网 |
| R10 | 范围蔓延反噬 router 主线 | 中 | 中 | 独立包 + 独立命令 + archtest 边界；随时可整体砍掉 |

---

## 10. 盲区与已知限制

诚实列出 VMR 这个位置**看不到**的东西，避免报告读者把"没记录"误读成"没发生"：

| 盲区 | 影响 | 能否缓解 |
| --- | --- | --- |
| 工具执行本身 | 只能看到耗时和返回文本，看不到副作用（写了什么文件、跑了什么命令的真实效果） | 不能。报告须显式声明"工具结果是 Agent 自述" |
| 不经 VMR 的调用 | Agent 若对某些任务直连其他 provider（如本地小模型做 compaction），这段完全不可见 | 部分：manifest 会出现无法解释的 `splice`/`contract`，应标为"外部编辑，来源不可见" |
| Agent 的内部状态 | 计划、记忆检索、循环检测等不体现在 messages 里的逻辑 | 不能。只能从行为反推 |
| 客户端重试与本地缓存 | 客户端自行重试可能不产生新请求 | 部分：`X-Stainless-Retry-Count` 头可见 |
| 被 VMR 拒绝的请求 | 有记录（`outcome=error`），但无上游往返 | 已覆盖，须计入事件流（Agent 的重试行为本身有信息量） |
| 跨 Agent 的人类协调 | 用户把 A 的输出粘给 B | 部分：内容 hash 相同即可发现，属于 Phase F 的额外能力 |

其中第二条是唯一需要在设计上留口子的：**编辑分类必须有"来源不可解释"这个显式类别**，不能强行归到某种已知 compaction。

### 10.1 暂缓项：Subagent

不做，但 Event 模型预留 `parent_step_id`，渲染层预留分支。识别信号（三条合取，均可验证）：

1. system blob 与主 journey 同源（共享 system hash 或工具集高度重叠）；
2. 时间窗**完整嵌套**在主 journey 某次 Task/agent 类 `tool_call` 与其对应 `tool_result` 之间——F9 已证明这对配对是精确的，所以这个窗口是**准确的区间**，不是估计；
3. 其最终输出 blob 出现在主 journey 后续那条 `tool_result` blob 里。

第 3 条是决定性的，与 F2 的 hash 匹配同一思路。Phase E 开工前先写采样脚本验证三条信号在现有语料上的命中率，验证不过就继续推迟。

---

## 11. 决策记录

v2.0 遗留的五个未决问题已于 2026-07-28 全部拍板，决策与理由见 **附录 C.1**，落地安排见附录 C 的分步计划。摘要：

| # | 问题 | 决策 |
| --- | --- | --- |
| D1 | Journey 断头（头不在输入集合里） | 默认跳过，`--include-partial` 才处理；~~ID 加 `t-` 前缀自证~~ **⚠️ 未实现**（2026-07-29 核对：`deriveID` 对断头/非断头 journey 一视同仁，都是 `j-<client>-<起始>-<结束>-<code>`，没有任何标记区分——`-include-partial` 渲染出的文件目前**看不出**自己是断头的，得打开正文看开头那句 `⚠ 本journey的开头是从上一段上下文断裂而来` 才知道。建议第 2 步或近期顺手补上，成本很低）；**缓存 key 用 step hash 而非 journey id**，因此 ID 不稳定不再是问题（这条仍成立，第 2/3 步引入 LLM 注解缓存时适用） |
| D2 | `contract` / `fork` 阈值 | 代码常量，不进 `config.yaml`；用全语料实测值定初值（附录 A.7） |
| D3 | 同一 chat 内连续任务是否合并 | **Journey 边界 = manifest 编辑边界，与时间无关**——Agent 是否保留上下文，就是它对"是否同一任务"的表态。不需要时间阈值 |
| D4 | 间隙分类边界 / 无人值守 | `heartbeat` / `dream_diary` 等 scheduled 类不进 story；间隙分类规则自洽，无人值守场景自动归 Agent 侧 |
| D5 | 非 OpenClaw profile 优先级 | 先按 OpenClaw 实现，架构留 profile 接口；之后跑 Pi / Hermes 看结果再决定是否写专用 profile |

---

## 附录 A：实测数据与复现方式

全部脚本在 `reports/`（`vmr report logs/vmr*` 的产物）上直接运行。

### A.1 末轮上下文的信息丢失（§2.3 / F1）

按 `vmr-requests.jsonl` 的 `session` + `detail_file` 取该会话全部 `details/*.json`，每条消息做 `md5(canonical json)`，比较"全部请求去重消息集合"与"末轮消息集合"：

```
s231  reqs= 24 union=  86 last=   8 dropped=  78  chars 165K/203K -> 81.4%
s290  reqs=444 union=1034 last=   5 dropped=1029  chars 2562K/2582K -> 99.2%
s187  reqs=254 union= 562 last= 394 dropped= 168  chars 697K/1414K -> 49.3%
s407  reqs=342 union= 743 last= 682 dropped=  61  chars 138K/531K -> 26.1%
s634  reqs=251 union= 483 last= 222 dropped= 261  chars 1126K/1393K -> 80.8%
s217  reqs=366 union=1303 last= 449 dropped= 854  chars 1844K/2258K -> 81.7%
```

### A.2 Compaction hash 缝合（F2）

`20260716-155251.355`（post-compaction，60 消息，#1 为 summary），取 `[2:]` 的 58 条 hash，与当日更早 581 条请求求交：

```
match 42/58  20260716-152238.775   <- s231，唯一强峰
match 40/58  20260716-152233.327
match 38/58  20260716-152229.810
match 36/58  20260716-152226.699
match 35/58  20260716-152145.349
match 31/58  20260716-152130.915
```

### A.3 lineage 内部的隐藏断裂（F6）

`s231` 逐轮 manifest 演化（`msgs` 含 system；`cover` = 与上一轮的 hash 集合覆盖率）：

```
15:17:29 turn= 1 msgs=  3 lcp=  0 cover=0.00
15:18:06 turn= 2 msgs=  5 lcp=  2 cover=0.60
   …（turn 3-19 单调 append，cover 0.71 → 0.97）…
15:22:38 turn=20 msgs= 79 lcp= 76 cover=0.97
15:51:22 turn=21 msgs=  4 lcp=  0 cover=0.25 <== BREAK
15:51:27 turn=22 msgs=  6 lcp=  4 cover=0.67
15:51:30 turn=23 msgs=  6 lcp=  6 cover=1.00
15:51:53 turn=24 msgs=  8 lcp=  6 cover=0.75
```

### A.4 S2 型「原地替换」compaction（F11）

`s231` turn 20 → turn 21 的头部对照：

```
turn 20 (79 msgs): #0 system    h=0b0a6cd7ee28  36579 字符
                   #1 user      h=6eb2c49e49ea    442 字符  [Thu 2026-07-16 15:17 …] 深入调研这个内存涨价…
                   #2 assistant h=3d6042de2c0e    120 字符
turn 21 ( 4 msgs): #0 system    h=8c78dc78bcb0  34060 字符  ← system prompt 变了
                   #1 user      h=6eb2c49e49ea    442 字符  ← 逐字未变
                   #2 assistant h=ed195ccf9337   2153 字符  ← 被改写，吸收摘要
```

中间 76 条工具调用/结果被这一条改写后的 assistant 消息取代。

### A.5 因果配对与规模（F9 / F12）

```
20260716-152238.775: messages 79  tool_calls 57  tool_results 57
                     ids matched 57 / orphan calls 0 / orphan results 0
语料: 7112 records, 808998 message instances, avg 113/request
审计源: 15 文件, zstd 后 124MB
```

### A.6 时间三分解（F10）

见 §4 F10 表格。间隙定义 `gap = ts(N+1) − (ts(N) + dur_ms(N))`，统计时剔除 `gap > 600s` 的样本（那些是人类空闲，需按 F10 的规则单独分类）。

### A.7 编辑类型的全语料分布（决定第 1 步范围的关键数字）

对全部 752 个 session 中请求数 ≥3 的 168 个，按时序遍历相邻 manifest 并分类（判据即 §2.2 的类型学，阈值取 `contract: len < 0.6×prev`、`fork: cover < 0.5`）：

```
edge_append            6045   95.86%
edge_splice_or_tail     202    3.20%
edge_contract            27    0.43%
edge_fork                32    0.51%

多轮 session          168
含至少一次断裂         27  (16.1%)
断裂最多的会话        s290(444 请求, 10 次)、s248(46, 8)、s70(9, 6)、s217(366, 3)
请求数 ≤2 被跳过      584   （单发定时脚手架，按 D4 本就不进 story）
```

三条结论：

1. **95.86% 的相邻边是纯 append**，83.9% 的多轮任务全程无断裂 → 第 1 步只处理单 lineage 就能覆盖绝大多数真实任务；
2. 真正的断裂只有 59 条边（`contract` 27 + `fork` 32），且**高度集中在少数长会话**里 → 缝合是高价值但低频的工作，适合放到第 2 步；
3. `splice_or_tail` 的 202 条是**混合桶**（真正的原地替换 + 正常的 ephemeral tail 替换），第 2 步需要把它拆开——这是第 2 步唯一需要新判据的地方。

阈值取值即上表所用值，直接作为代码常量初值（D2）。

### A.8 语料构成

| 维度 | 分布 |
| --- | --- |
| 协议 | openai 7053 / anthropic 59 |
| 调用方 | lobster 1860、pimini 1310、hermes 764、openclaw 663、pi 462、workbudy 61、其余 17 |
| 工作负载 | interactive 6555、heartbeat 324、dream_diary 224、compaction 9 |
| 会话 | 752（最大 444 请求 / 32 任务 / 跨 5 天） |

---

## 附录 B：v1.0 → v2.0 修订清单

本轮 review 在源码与数据上重新推导后，推翻了 v1.0 的三处结构性判断，补了两处遗漏，并修正了一处夸大。

| # | v1.0 的说法 | v2.0 的修正 | 依据 |
| --- | --- | --- | --- |
| 1 | LLM 解读层是核心价值，剖面指标未提及 | **规则派生的行为剖面才是核心价值**，LLM 是可选的第三层 | §3.2 拆解 deepseek 报告，其 90% 证据来自规则可得的表格 |
| 2 | compaction 靠识别模板检测 | **靠检测非追加型 manifest 编辑检测**，模板只是补充证据 | F11：至少三种形态，其中两种没有任何模板可认 |
| 3 | `story` 不依赖 `report`，各写一套 | **两者共同依赖更低的 `ctxgraph` 层**；v1.0 会造成 lineage 逻辑永久二重实现 | §2.5 / §7.1：manifest 逻辑已存在于 `report`，只是被降级成私有字段 |
| 4 | （无） | 新增 **F10 时间三分解**：墙钟时长作为效率指标几乎无意义，净工作时长才是 | 实测 s187 有 90% 墙钟是人不在场的空等 |
| 5 | （无） | 新增 **F6 补充 / R2**：缝合不只要合并，还要会**切分**；anchor 会把两段 lineage 粘在一起 | 实测 `s231` turn 20→21 的硬断裂 |
| 6 | （无） | 新增 **F11 `revision` 关系**：原地改写的消息若不标注，叙事会读成"同一件事说了两遍" | 实测 `s231` #2 消息 hash 变更 |
| 7 | 传播价值成立，脱敏"延后到有需求再做" | **两者矛盾**；脱敏是传播价值的前置条件，Phase D 之前传播价值记 0 | §5.4 |
| 8 | 合计 14.5 人天 | **20.5 人天**；v1.0 低估，因为它没算 `report` 迟早要重做同一件事的成本 | §8 |
| 9 | 分期 P0–P4 | 重组为 A–F，新增 Phase B（技术债偿还）与 §7.2 的**一致性测试防分叉机制** | §7.2 / R8 |

---

## 附录 C：分步实施计划（可直接据此开工）

### C.1 已确认的决策与理由

**D1 — 断头 Journey：默认跳过，且这个问题比看上去小。**

原始担忧是"journey 的头不在输入集合里会导致 ID 不稳定，缓存和外链失效"。重新推导后发现担忧的前半段成立、后半段不成立：

- ID 确实会变（内容寻址的 ID 依赖于"最早可见的 manifest"）；
- 但**缓存不受影响**——LLM 注解的缓存 key 是 `(step_hash, model, prompt_version)`，`step_hash` 由消息内容决定，与 journey 边界无关。加载更多历史文件后，同一个 Step 的注解照样命中缓存。
- 剩下的只有"外链失效"（有人存了 `journey-xxx.md` 的路径），这个代价可以接受。

因此方案（成本≈0）：

1. **默认跳过断头 journey**，日志里给一句 `skipped N head-truncated journey(s), use --include-partial to render them`；
2. `--include-partial` 时才渲染，ID 加 `t-` 前缀（如 `t-a91f3c…`）——**前缀本身就是"这个 ID 不保证稳定"的自证**，不需要额外文档；
3. 断头判据：lineage 的首 manifest 不是"冷启动形态"（消息数 ≤3 且无 tool_result），且它在输入文件集合的**第一个文件的前若干行内**（说明历史被输入范围截断，而非真的从这里开始）。

**D2 — 阈值是代码常量。** 放 `internal/ctxgraph/edit.go` 顶部，附一句注释说明取值来自哪次实测。初值取附录 A.7 实测所用值：

```go
// Thresholds are calibrated against the 2026-07-14..28 corpus (7112 records,
// 168 multi-turn sessions) — see docs/…_opus-5.md 附录 A.7 for the resulting
// edit-kind distribution. Tune here when more corpora disagree; deliberately
// NOT a config knob (users cannot calibrate what they cannot measure).
const (
    contractLenRatio = 0.6 // |cur| < 0.6*|prev| → contract
    forkCoverage     = 0.5 // |cur ∩ prev| / |cur| < 0.5 → fork
    tailSlack        = 2   // LCP within this of |prev| still counts as append
)
```

**D3 — Journey 边界 = manifest 编辑边界，与时间无关。** 这条同时解决了"用户 2 小时后回来追问"的场景，而且不需要任何时间阈值：

- 用户走开 2 小时后回来追问、Agent 带着完整历史继续 → manifest 是 `append` → **同一 journey，新 task**；
- 用户开一个无关的新任务、Agent 清空或重建上下文 → `contract` / `fork` → **新 journey**。

**Agent 是否保留上下文，就是它自己对"这是不是同一个任务"的表态。** 我们不需要判断语义，只需要读取它的表态。这比任何时间阈值都准确，而且零参数。

残留边界情况：Agent 在同一 chat 里不清空上下文就接了个完全无关的新任务 → 会并成一个 journey。此时 task 分章已经表达了边界，报告里额外给一个提示（新指令与前文内容重叠度极低）。可接受，不特殊处理。

**D4 — scheduled 类不进 story；间隙分类规则自洽。**

- `heartbeat` / `dream_diary` 等单发脚手架整体排除（复用 `report` 已有的 workload class 判据）。附录 A.7 里那 584 个"请求数 ≤2 被跳过"的 session 基本就是这一类，本来也进不来。
- 间隙分类规则：**间隙之后紧跟一条新的真实用户指令 → 人类空闲；否则 → Agent 侧执行。** 夜间无人值守长任务不会出现新用户指令，因此自动归为 Agent 侧执行——规则自洽，不需要特判。
- 间隙后既有新用户指令又有工具循环：按"该请求的 delta 里是否含新真实用户指令"判定，`deltaHasNewInstruction()` 的现成语义直接可用，不引入新判据。

**D5 — OpenClaw 优先，架构留 profile 接口。** 第 1 步就要把 agent 特化收敛到一个接口后面，但只实现 OpenClaw + 一个通用兜底：

```go
// internal/story/profile/profile.go
type Profile interface {
    Name() string
    Detect(m *ctxgraph.Manifest) bool          // 是否由本 profile 处理
    IsRealUser(msg chatmsg.Message, raw any) bool  // 剥离路由头/脚手架
    CompactionMarker(msg chatmsg.Message) bool     // 首条非 system 消息的严格判定（F5）
    NoReply(respText string) bool
}
```

内核（`ctxgraph`）完全不认识 profile——它只做 manifest 差分。profile 只影响 `story` 的 task 切分与标记识别。这样跑 Pi / Hermes 时，最坏情况是走通用兜底、task 标题差一点，**事件流和编辑分类照样正确**。

---

### C.2 总体节奏

| 步 | 主题 | 一句话目标 | 人天 | 交付后能做什么 | 状态 |
| --- | --- | --- | ---: | --- | --- |
| **1** | **能看** | 单 lineage 的事实层叙事报告，切分但不缝合 | **3.5** | 对 84% 的多轮任务出完整报告，16% 出分段报告 | ✅ **已完成**（2026-07-28 实现 + 2026-07-29 评审整改/用户反馈增补，见附录 D/E/F/G） |
| **2** | **能信 + 能比** | 跨 compaction 缝合 + 行为剖面 + 一致性测试 | **5** | 100% 覆盖；两份剖面做差即可对比两套 Agent | ✅ **已完成**（2026-07-29/30 实现 + 真实语料验证，见附录 I） |
| **3** | **能还债** | `report` 迁移到 `ctxgraph`，修三个已知缺陷 | **3** | 一套 lineage 模型两个渲染器；report 自身变准 | ⬜ 未开始 |
| **4** | **可选扩展** | 四个互不依赖的模块，按当时需要挑 | **9** | LLM 解读 / HTML+脱敏 / subagent / 对比报告 | ⬜ 未开始 |

前三步共 **11.5 人天**，构成完整闭环。第 4 步的四个模块彼此无依赖，可以任意挑、任意顺序、随时停。

---

### C.3 第 1 步 —— 能看（3.5 人天）

> **状态：✅ 已完成**（2026-07-28 实现，2026-07-29 代码评审整改 + 用户反馈增补）。下面每项任务/验收标准后标注了核对结果；两处**实际偏离原计划**（CLI selector 简化、验收标准 #3 的自动化断言未落地）已用 ⚠️ 标出，详细分析见附录 G。

**目标**：`./vmr story --session s231 logs/vmr*` 能产出一份可读的任务全流程 Markdown，零 LLM 成本，不动 `report` 一行代码。

**为什么这是 low hanging fruit**：附录 A.7 证明 95.86% 的相邻边是纯 append、83.9% 的多轮任务全程无断裂。**缝合是贵的，切分是免费的**（LCP + 集合覆盖率，O(n) 纯函数）。所以第 1 步只切不缝：断裂处切开、各自成篇、显式标注，于是 100% 的任务都拿得到可用产物，只是 16% 会分成几篇。这比"遇到断裂就报错"好，也比"硬缝"安全。

#### 任务清单

**T1.1 `internal/chatmsg` 下沉（0.5 天）—— ✅ 已完成**

从 `internal/report/render.go` 搬出纯函数，不改行为：

| 新文件 | 内容 | 来源 |
| --- | --- | --- |
| `messages.go` | `Message` / `Messages(body any) []Message` / `RenderContent` / `RenderPart` / `ImagePlaceholder` / `ToolNames` / `MsgOffset` | `render.go:110-215, 381-419` |
| `sse.go` | `StreamSummary` / `ToolCall` / `ReassembleSSE` / `FinalMessage` | `render.go:420-580` |
| `*_test.go` | 对应测试搬迁 | 现有 `render` 相关测试 |

注意：`imagePlaceholder` 依赖 `fmtBytes`，下沉后改用 `internal/fmtutil.FmtBytes`（该包存在正是为了这种共享）。`report/render.go` 保留同名非导出薄包装委托过去，把改动面压到最小。

**验收**：`go test ./internal/report/... ./internal/chatmsg/...` 全绿；`vmr report` 对同一批日志的输出与迁移前**逐字节相同**（用 `diff -r` 比对 `reports/`）。

**T1.2 `internal/ctxgraph` 内核（1.5 天）—— ✅ 已完成**（⚠️ 只实现四种编辑：`Append`/`ReplaceTail`/`Contract`/`Fork`，`Splice` 按计划本身"明确不做"清单里的说明并入 `ReplaceTail`，留给第 2 步 T2.1 拆分——这是计划自己写清楚的范围，不是遗漏）

| 文件 | 内容 |
| --- | --- |
| `hash.go` | `type Hash [16]byte`；`HashMessage(raw any) Hash` = `md5(json.Marshal(raw))`，**必须与 `session.go:399-403` 的 canonical form 逐字一致**（Go 的 `json.Marshal` 对 map 键排序，摘要确定） |
| `manifest.go` | `Manifest{Path, Line, TS, SysHash, Keys []Hash, LeadSys int, Model, Protocol, Outcome, Endpoint, DurMS, TTFTMS int64, Usage}`；`BuildManifest(rec *audit.Record, path string, line int)` |
| `edit.go` | `EditKind`（`Append`/`ReplaceTail`/`Splice`/`Contract`/`Fork`）；`Classify(prev, cur *Manifest) Edit{Kind, LCP, Coverage}`；D2 的阈值常量 |
| `blobindex.go` | `Index map[Hash]BlobRef`（`BlobRef{Path, Line, Idx}`，只记首次出现）；`Fetch(refs []BlobRef) (map[Hash]any, error)` —— **按 Path 分组、每文件一次顺序扫描**（zstd 不可寻址） |
| `lineage.go` | `Lineage{ID, Manifests, Edges}`；`Split(ms []*Manifest) []*Lineage` —— 先按 SessKey 聚合（沿用 anchor 规则），再按 `Contract`/`Fork` 切开 |
| `scan.go` | `Scan(paths []string) (*Graph, error)` —— 第一遍：只建 manifest + index，不留内容。复用 `audit.OpenLogFile` / `ForEachLine`，沿用 `AnalyzeSessions` 的有界 worker 池模式 |

**验收**：`edit_test.go` 用手工构造的 manifest 覆盖五种编辑各至少两例（含边界）；`Scan` 在全语料上跑通且峰值内存 < 200MB（附录 A.7/F12 预算：manifest 13MB + index 10MB）。—— ✅ 全语料（15 文件/7112 条记录）扫描零 panic，实测约 15 秒（2026-07-29 复测，与附录 D.2 记录的 57 秒是不同硬件/负载下的两次独立测量，量级一致）；`BlobIndex`/`FetchRecords` 后续（附录 F）加了并行 worker 池，进一步降到同量级。

**T1.3 `internal/story` 事实层 + Markdown（1 天）—— ✅ 已完成**（文件切法与计划略有不同：实际实现把 `task.go`/`event.go`/`step.go` 三个计划文件合并进了单一 `journey.go`——Journey/Task/Step/Event 四个类型加上 `Build` 都写在一起，因为 `Build` 一次遍历就要同时产出全部四层结构，拆成三个文件反而要在它们之间传递大量共享状态；功能范围与计划一致，只是文件粒度更粗）

| 文件 | 内容 |
| --- | --- |
| `journey.go` | `Journey`；第 1 步 `1 lineage == 1 journey`；ID 派生（D1）；断头判定与默认跳过 |
| `task.go` | Task 切分：delta 内出现新真实用户指令即开新 task（`profile.IsRealUser`） |
| `event.go` | `Event{Kind, Hash, FirstStep, LastStep, GoneReason, Ref}`；`BuildEvents` 用全局 seen-hash 去重 |
| `step.go` | `Step` = 一次请求往返（新增 events + 该请求响应 + 时长/token/endpoint） |
| `profile/` | `profile.go`（接口，见 D5）+ `openclaw.go` + `generic.go` |
| `render_md.go` | Markdown 渲染，原文用 `<details>` 折叠 |

**渲染骨架（原计划，2026-07-28，仅供对照）**：

```
# Journey <id> · <标题> · <起止时间>
> N tasks · M steps · 净工作时长 X · 模型时间 Y
> ⚠ 本 journey 在 <时间> 处上下文被重建，与后续片段的关系待确认（第 2 步补齐）

## t01 · <用户指令预览>
### Step 1 · 15:17:29 · 37.1s (ttft 30.4s) · 27.6K/256/1.5K
  ▸ user: …（首次出现的用户消息）
  ▸ assistant: …
  ▸ tool_call web_fetch(id=call019f…) → tool_result (4.4KB)   ← 配对由协议给定
  <details>原始 body</details>
```

**实际骨架（2026-07-29，附录 F.2/F.4 改动后，以此为准）**——⚠️ **第 2 步/后续步骤开工前必读，不要照抄上面那份已过期的原计划骨架**：ID 换成了 `j-<client>-<起始时间>-<结束时间>-<code>`（附录 F.2），文件名同步跟着变；`tool_call`/`tool_result` 不再是一行摘要，拆成了 **Messages**（本轮新进入上下文的内容）和 **LLM Response**（本轮模型自己产出的内容：reasoning、回复文本、每个 tool_call 的完整 name/id/参数）两段（附录 F.4）；编辑行的 `lcp`/`cov` 缩写换成了人话：

````
# Journey j-pimini-20260724T125852-20260724T141928-aca17c1f

> read both @REPORT_REDESIGN.zh.md and @REPORT_REDESIGN_V2.zh.md 你看一看V2当中指出的问题是不是有…

> 3 任务 · 105 轮 · 2026-07-24 12:58:52 → 14:19:28

## t02 · read both @REPORT_REDESIGN.zh.md and @REPORT_REDESIGN_V2.zh.md 你看一看V2当中指出的问题是不是有…

### Step 4 · 21:32:26 · 2.9s (ttft 2.0s) · 135/25.2K/86 · openai:volcengine:glm-5.2

> 编辑: append（最长相同前缀 6 条消息，内容重合率 75%）

**Messages**

<details><summary>▸ assistant · Translated and saved to `…REPORT_REDESIGN.zh.md`. The tra…</summary>
```
（原文……）
```
</details>

<details><summary>▸ user · read both @REPORT_REDESIGN.zh.md and @REPORT_REDESIGN_V2.zh.md 你看一看V2当中指出的问题是不是有…</summary>
```
（原文……）
```
</details>

**LLM Response**

<details><summary>🤔 reasoning · 170 字符</summary>
```
The user wants me to read both files and evaluate…
```
</details>

<details><summary>finish: tool_calls (read, read)</summary>

🔧 **tool_call** `read` [id=call_6cedd1881aad4a8d80063471]
```json
{
  "path": "/Volumes/SSD2T/code/vmr/REPORT_REDESIGN.zh.md"
}
```

🔧 **tool_call** `read` [id=call_5c7cc8fa23e84b1dae853464]
```json
{
  "path": "/Volumes/SSD2T/code/vmr/REPORT_REDESIGN_V2.zh.md"
}
```
</details>
````

（截取自真实渲染出的 `journey-j-pimini-20260724T125852-20260724T141928-aca17c1f.md`，即 §0 场景 S-C 的一个真实事故复盘案例。）

**T1.4 CLI 与边界（0.5 天）—— ✅ 已完成**（⚠️ selector 与计划不同，见下）

- `cmd/vmr/cmd_story.go`：~~`vmr story [--session sNN|--journey jNN|--chat <id>|--since|--until] [--include-partial] [-o dir] <file|glob>...`~~ 实际落地为 `vmr story [-c config.yaml] [-journey id | -render-all] [-include-partial] [-show-ungrouped] [-o dir] [audit.jsonl|glob]...`——`--session`/`--chat`/`--since`/`--until` 四个 selector 均未实现，只保留 `-journey`（按 id 前缀渲染单个）。原因：`--session sNN` 用的是 `report/session.go` 自己的编号体系，`ctxgraph.Lineage` 是完全独立的分组，两者的 id 空间对不上，第 1 步没有（也不该）为了兼容一个别的系统的编号去污染 `story` 的接口；`--since`/`--until` 目前可以用"传一个子集文件 glob"等价替代，暂无独立实现，如果后续觉得常用可以再补。新增了计划里没有的 `-render-all`（一次渲染全部候选，见附录 F.3）和 `-c`（默认输入路径解析，见附录 F.1）
- 未给 selector 时：列出候选 journey（ID / 标题 / 步数 / 起止 / 是否断头）后退出，不渲染——避免误跑全量 —— ✅
- `internal/archtest/import_boundaries_test.go` 新增：`ctxgraph` 不得依赖 `router`/`server`/`config`/`report`/`story`；`story` 不得依赖 `router`/`server`/`report` —— ✅
- 产物权限 0600 / 目录 0700 —— ✅
- `vmr.sh` 无需改动（`passthrough` 自动转发）—— ✅

#### 验收标准（可执行）

1. ~~`./vmr story --session s231 logs/vmr*` 产出 2 篇~~ —— **等价验证方式已通过**：real corpus 上 `-journey` 精确复现了设计阶段手工找到的 s231→s238 断裂（附录 D.2，`j-0e0358966da4`），selector 本身按上面的说明改成了 `-journey`；
2. 全语料扫描不 panic（原文写 `--since/--until` 扫描，该 selector 未实现，此处验证的是不带任何 selector 的全量 `Scan` 路径）—— ✅；总耗时 ≤ 现有 `vmr report` 的 2 倍 —— ✅ **实测**（2026-07-29，同一份 15 文件真实语料）：`vmr report` 51.8s，`vmr story`（列表模式）15s、`-render-all`（253 个候选全渲染）65s，两者都远低于 2× 上限；
3. **不变量断言**：事件流内 `tool_call` 与 `tool_result` 的 id 配对率必须 100%，不满足即 `t.Fatal`（守住 F9）—— ⚠️ **未落地为自动化测试**。附录 A.5 用手工脚本在一条真实记录上验证过 57/57 配对，且 F9 的因果关系是协议给定、理论上不会出错，但从未写成一条持续跑在语料上的回归测试。这条正好是第 2 步 R1（缝合误判）的风险缓解**直接引用**的事实（"F9 已证明 Step 内因果是零误差的，不确定性只作用在跨 Step 的 lineage 关系上"）——第 2 步要往这个假设上面加缝合逻辑，建议把这条断言补上作为第 2 步的前置任务，而不是继续放着，详见附录 H；
4. golden test：`testdata/` 里一份 3 请求的小语料，Markdown 输出逐字节稳定，重跑幂等 —— ✅（2026-07-29 补齐，见附录 E.2.D，过程中还抓到一个真实渲染 bug，见 E.3）；
5. `go test ./internal/archtest/...` 全绿 —— ✅；
6. `diff -r` 证明 `vmr report` 输出未受影响 —— ✅（附录 D.4：唯一的差异是一个与本功能无关的预置缺陷，顺手修了）。

#### 明确不做

不缝合（断裂只切不接）· 无剖面层 · 无 LLM · 无 HTML · 不动 `report` · 不处理 subagent · 断头 journey 默认跳过 · 不拆 `splice_or_tail` 混合桶（全部按 `ReplaceTail` 处理，留给第 2 步）。—— ✅ 全部按计划保留至今，无一项在第 1 步的后续增补中被提前实现；下面附录 F 的四项增补全部是 CLI 易用性/渲染质量层面的改动，没有触碰这条边界。

---

### C.4 第 2 步 —— 能信 + 能比（5 人天）

> **状态：✅ 已完成（2026-07-30）**——按附录 H.5 的优先级顺序（T0 前置任务 → T2.5 → T2.1 → T2.2 → T2.3 → T2.4）全部实现并通过真实语料验证，完整记录见附录 I。下面的任务清单/验收标准保留原文（含开工前的核对结论），逐项完成情况见附录 I.4。

**目标**：断裂能缝上且缝得可信；两份 journey 的剖面做差就能回答"这两套 Agent 差在哪"。

#### 任务清单

**T2.1 拆开 `splice_or_tail` 混合桶（0.5 天）**
附录 A.7 里 202 条边混了"真原地替换"（F11 的 S2）与"正常 ephemeral tail 替换"。判据：`Splice` = 公共前缀之后有新 blob，**且原尾部的一段在新 manifest 里原样重现**（中段被换）；`ReplaceTail` = 公共前缀之后无原尾部重现（只是尾巴被改）。先在全语料上跑一遍分布，把结果写回附录 A.7。

**T2.2 缝合（1.5 天）** — `internal/ctxgraph/stitch.go`
blob 倒排索引（`Hash → []LineageID`）→ 对每个 lineage 首 manifest 找覆盖率最高的前驱 → 四类边（`splice` / `compaction` / `head_prune` / `same_chat`）各带 `evidence` 与 `confidence`。`same_chat` **默认不缝合**，只标注"疑似同源"。缝合失败时区分三态（F4）：已缝合 / 确认无后继 / 匹配失败。
> ⚠️ **新增设计点**（第 1 步之后才出现，原计划未覆盖）：附录 F.2 把 Journey id 从纯哈希改成了 `j-<client>-<起始时间>-<结束时间>-<code>`，现在是**单个 lineage** 的属性。缝合之后一个 Journey 可能横跨多个 lineage，id 该取"最早 lineage 的起始 + 最晚 lineage 的结束"，还是保留"根 lineage 的 id 不变、缝合关系另外记录"，这里需要在 T2.2 开工时一并定下来，不定会导致缝合前后同一 journey 的文件名/id 对不上。
> ✅ **已定案（D-ID，附录 I.2）**：取整条缝合链最早 lineage 的 client + code，覆盖到最晚 lineage 的结束时间——`deriveID` 从"单 lineage"泛化为"lineage 链"。

**T2.3 Compaction 三形态与信息损失（1 天）** — `internal/story/compaction.go`
S1/S2/S3 识别（F11）+ `revision` 关系标注（避免"同一件事说两遍"）+ system 变更事件 + 信息损失度量（token 前后对比 + 被吞段的文件路径/工具名/URL 在 summary 中的存活率，纯规则）。

**T2.4 行为剖面（1 天）** — `internal/story/metrics.go`
§6.5 的九项指标，其中 F10 时间三分解按 D4 的规则分类间隙。输出同时落 `journey-<id>.json`，供第 4 步的对比模块直接消费。

**T2.5 一致性测试（1 天）** — `internal/story/conformance_test.go`
**这是 R8 的唯一保险，不可延后。** 用 `ctxgraph` 重建的 session 分组与 `report.AnalyzeSessions` 在同一语料上逐条比对，每一处差异必须落到两个白名单之一：

- `expectedSame`：应当一致，不一致即失败；
- `knownImprovement`：ctxgraph 修正了 report 的已知缺陷，每条附 F 编号（如 F6 的 lineage 粘连、F2 的 compaction linking）。

出现第三种差异 = 测试失败。有了它，第 3 步是机械迁移；没有它，第 1/2 步会变成永久分叉。
> ⚠️ **优先级提醒**：§7.2/R8 原文写的是"必须与 Phase A 同批交付，不可延后"，但可执行版计划（本附录）把它排进了第 2 步——第 1 步已经在没有这项测试的情况下交付，R8 现在处于**未缓解状态**。建议 T2.5 在第 2 步里第一个做（或者作为第 2 步开工前的独立小任务），而不是按 T2.1→T2.5 的顺序排到最后。详见附录 H 的风险分析。
> ✅ **已按建议提前执行**：连同验收标准 #3 的补课（`chatmsg.CheckToolPairing`）一起排在 T2.1 之前完成，R8 现已缓解，详见附录 I.2。

#### 验收标准

1. `vmr report` 现在打的 8 条 `compaction linking` 告警，全部落入"已缝合"或"确认无后继"，无"匹配失败"；
2. s231 的两段被缝成一个 journey，边类型 `compaction`，置信度高，证据可打印；
3. 对 §4 F1 的 6 个会话各出一份剖面，人工核对净工作时长与 A.6 的手算一致；
4. 一致性测试通过，且 `knownImprovement` 白名单里每条都有 F 编号。

---

### C.5 第 3 步 —— 能还债（3 人天）

**目标**：一套 lineage 模型，两个渲染器。`report` 自身的三个缺陷一并修掉。

- **T3.1（1.5 天）** `internal/report/session.go` 的分组改为消费 `ctxgraph.Lineage`，删除私有 `keys` 字段与 `lcp()`；`linkCompactions()` 改为读 `ctxgraph` 的缝合边；`session_test.go`（534 行）按一致性测试的白名单更新。
- **T3.2（0.5 天）** 修 `metrics.go` 的 `context_growth`：改为"按 lineage 分段计算，取最长段"，消除 `s559` 的 ×179.5 脏值（§7 现在自己标着"中途 compaction"）。
- **T3.3（1 天）** 新增 `internal/report/section_compaction.go`（= CCR N-4）：本期发生 N 次 compaction、每次前后 token、信息损失率、哪些虚拟模型最容易触发。**新增一节 = 新增一个文件**，符合 `archtest` 的行数预算约束。同步更新设计文档 §9.4 的章节清单与 `Format` 版本号。

**验收**：一致性测试仍全绿；`vmr report` 对全语料输出的 §6 会话数与第 2 步的 journey 数可解释地对应；`go test ./internal/archtest/...` 全绿。

---

### C.6 第 4 步 —— 可选扩展（9 人天，四个模块互不依赖）

| 模块 | 人天 | 触发条件 |
| --- | ---: | --- |
| **4a LLM 解读层** | 2.5 | 用了第 1–3 步之后仍觉得读起来累。含 prompt 设计、结构化输出、`(step_hash, model, prompt_version)` 落盘缓存、dry-run 预算与确认、端点缺失时整层降级 |
| **4b HTML + 脱敏** | 2.5 | 需要对外分享时。单文件自包含（内联 CSS/JS，零外部依赖）+ `--redact`。**在 4b 交付前，传播价值记 0**（§5.4） |
| **4c Subagent** | 2.5 | 先跑采样脚本验证 §10.1 三条信号的命中率，不达标就继续推迟 |
| **4d 双 Journey 对比** | 1.5 | 第 2 步的剖面 JSON 落地后即可做——两份剖面做差 + 差异归因表 |

建议顺序：先 4d（最便宜且直接命中原始动机），再按需 4a / 4b / 4c。

---

### C.7 贯穿所有步骤的不变量

开工前贴在显眼处，每次 PR 自查：

1. **三层不得混淆**：事实层（规则）→ 剖面层（规则派生）→ 解读层（LLM）。**LLM 不得生成任何数字**，注解必须携带引用的 step id。
2. **宁可断开，不要错连。** 低置信度的边渲染成显式断裂标记，不静默合并。切分同理（R2）。
3. **不变量断言**：`tool_call` ↔ `tool_result` 配对率 100%（F9）。这是唯一零误差的事实，用测试守住。
4. **阈值是代码常量**，附实测出处注释，不进 `config.yaml`（D2）。
5. **产物 0600 / 目录 0700**，与 `details/` 同级——它们携带完整对话正文。
6. **archtest 边界**：`ctxgraph` 不依赖 `router`/`server`/`config`/`report`/`story`；`story` 不依赖 `router`/`server`/`report`；`report` 从第 3 步起可依赖 `ctxgraph`。
7. **幂等可重跑**：同一输入产出逐字节相同（LLM 注解靠缓存保证）。
8. **"来源不可解释"是一等公民**（§10）：无法归类的编辑标为该类别，不强行塞进某种已知 compaction。
9. **内容不驻留内存**：只索引 `(path, line, idx)`，按需按文件顺序回捞；zstd 不可寻址是硬约束。

---

## 附录 D：第 1 步实施记录（2026-07-28 完成）

第 1 步（附录 C.3「能看」）已实现并通过全面代码走查 + 真实语料验证，记录如下，供后续步骤（第 2/3 步）参照。

### D.1 交付物

新增 `internal/chatmsg`（消息/SSE/usage 解析，从 `report` 下沉）、`internal/ctxgraph`（manifest 哈希/编辑分类/lineage 切分/记录回捞）、`internal/story` + `internal/story/profile`（Journey/Task/Step/Event 构建与 Markdown 渲染）、`cmd/vmr/cmd_story.go`（`vmr story` 子命令）；`internal/report` 新增 `chatmsg_compat.go` 委托层与 `ctxgraph_parity_test.go`（仅测试文件依赖 ctxgraph，不影响 `archtest` 的生产依赖边界）；`internal/archtest` 新增 `ctxgraph`/`story` 的依赖边界规则。全部 89 个新增测试 + 现有测试通过（含 `-race`）。

### D.2 真实语料验证

- 精确复现设计阶段手工找到的 s231→s238 断裂（`j-0e0358966da4`，15:51:22 起 4 轮），并额外发现 OpenClaw 在真正 compaction 之前会先跑一轮"pre-compaction memory flush turn"——此前的手工分析未注意到这一细节。
- 全 15 天语料（7112 条记录）扫描零 panic，约 57 秒完成，产出 829 个 lineage。
- 渲染真实的 57 轮 A 股新股打新调研 Journey（`j-99aa3efefd21`），内容里直接看到 Agent 自己的一个真实 bug：把 `write` 工具的参数写成字面量 `"{result}"` 而非插值内容，导致文件只写入 8 字节——正是这个功能想要暴露的东西。

### D.3 走查发现并修复的问题

对全部新增/改动文件做了一次逐文件走查（不止是跑测试），发现并修复以下问题：

| # | 问题 | 严重度 | 修复 |
| --- | --- | --- | --- |
| 1 | `ctxgraph.BuildManifest` 把 system prompt 文本哈希写成了 `hashJSON(text)`（会先做 JSON 转义再哈希），而 `session.go` 是直接哈希原始字节——同一份 prompt 会算出不同哈希 | 高（破坏 §7.2 一致性测试的前提） | 改为 `md5.Sum([]byte(text))`，与 `session.go` 逐位对齐。**是 `ctxgraph_parity_test.go` 当场抓到的**——这也是提前引入这道校验（而非等到第 2 步）的价值所在 |
| 2 | `story.RenderMarkdown` 的事件渲染用固定 ` ``` ` 三反引号包裹消息原文，真实语料里已证实有消息本身含三反引号代码块（本次渲染的 57 轮 Journey 里就有 4 处），会提前把 fence 闭合、后续内容全部损坏 | 高（真实数据会触发，不是边界情况） | 移植 `report/render.go` 的 `codeFence`（动态取比内容中最长反引号串更长的 fence）。真实语料验证：该 Journey 里正确生成了 4 处 4-反引号 fence，`<details>`/`</details>` 配对 115/115 完整 |
| 3 | `ctxgraph.Classify` 的 Contract 判定用 `int(float64(len(prev))*0.6)` 做阈值，当 `len(prev)<=1` 时整数截断到 0，导致"1 条消息→0 条消息"这种彻底收缩反而分类不到 Contract、落到默认的 Append | 中（真实语料里 `len(prev)` 通常远大于 1，命中概率低，但逻辑上是真的错） | 改为全程用 float 比较（`float64(cur) < float64(prev)*0.6`），不再对阈值取整；已验证对 A.7 附录里所有真实边界数据的分类结果不变 |
| 4 | `story.deriveTitle` 有一个从未被读取的 `recs` 参数（函数体内实际用的是 `Step.Rec`） | 低（死参数，非功能性缺陷） | 删除该参数，简化签名 |
| 5 | `internal/ctxgraph/records.go` 文档注释写错方法名（`BlobIndex.Fetch`，实际是 `FetchAll`） | 低（纯文档） | 更正 |
| 6 | `cmd_story.go` 的 `Scan()` 结果里 `Graph.NoBody`/`Graph.Ungrouped` 算出来后从未打印，用户看不到有多少记录解析失败/未归组 | 低（可观测性缺口，非缺陷） | 在扫描后加一行汇总（`vmr report` 早已有的同类惯例） |

每条修复都补了对应的回归测试（`TestClassify_TinyPrevContractsCorrectly`、`TestRenderMarkdown_EmbeddedBackticksDontBreakTheFence`），并且对 #1/#2 做了"先证明测试会失败、再验证测试通过"的双向确认。

### D.4 顺带修复：`internal/report` 的一个预置缺陷（不在本次任务范围内，验证副产品）

验证「改动前后 `vmr report` 输出逐字节相同」时，用未改动的原始二进制在同一份输入上连续跑两次，发现 §7 效率表的"定时任务冗余"这一行会在 `heartbeat`/`dream_diary` 之间随机跳变——**与本次改动完全无关，改动前的原始代码就有这个问题**。

**根因**：`internal/report/aggregate.go` 里 `rep.Efficiency = buildFindings(rep)`（第 645 行）在"对所有切片排序"那一段（第 690+ 行，含 `rep.Workloads`/`rep.Tools`/`rep.ByModel` 各自的确定性排序）**之前**就跑了。`buildFindings`（`metrics.go`）里三处"遍历 map 转出来的切片、找第一个满足条件的就 break"的写法，读到的都是排序前的、Go map 遍历顺序（语言规范明确不保证跨进程稳定）产生的原始顺序——这正是 `TestBuildIsDeterministic` 那条注释描述的同一类问题，只是换了一层：它发生在"从数据里挑一条 finding"，而不是"渲染一行 row"。

**验证**：`rep.Workloads` 本身（JSON 里的数组，受后面的排序保护）在两次运行里逐字节相同；heartbeat 和 dream_diary 各自的 `TokensInFresh`/`CacheEfficiency` 数值也完全相同——问题只出在 `buildFindings` 从这份数据里"选哪一条来报告"这一步。

**修复**：把「工具 schema 浪费」「缓存未命中输入的 implicated 模型」「定时任务冗余」三处"遍历+第一个匹配就 break"改成显式地在整个集合上比较找最优（最大浪费字节数/最大新鲜 token 数），并各自加上以自身身份字段（Shape/Model/Class）为准的确定性 tie-break；顺手给「上下文膨胀（worst session）」也加了同样的 tie-break（此前虽已是显式取最大值，但精确相等时的胜出者仍依赖遍历顺序）。

新增 `TestBuildFindingsIsDeterministic`（`internal/report/aggregate_test.go`）：构造一对 heartbeat/dream_diary 记录，令两者 `cache_efficiency` 恰好精确相等（都设为 0）但 `fresh token` 数不同，跑 8 次 `Build()` 断言"定时任务冗余"永远指向 fresh 更高的那一个。**验证方法论**：临时用 `git stash` 把修复本身撤掉、跑同一条新测试 10 次，10 次里出现了 8 次失败（"heartbeat"/"dream_diary" 交替出现）；恢复修复后连续多次运行全部通过——证明这条测试确实会抓住这个 bug，不是摆设。真实语料上也复测了 5 次 `vmr report`，§7 表格逐字节完全一致。

这个修复严格限定在 `buildFindings` 函数内部（`internal/report/metrics.go`），不触及本次故事功能的任何生产代码路径，也不改变 `rep.Workloads`/`rep.Tools`/`rep.ByModel` 等切片本身的数据或排序逻辑——纯粹是"选 finding"这一步从隐式依赖调用时序，变成显式、自洽、不依赖调用顺序。

### D.5 结论

第 1 步在真实语料上验证充分：能正确切分 lineage、能正确复现已知的真实断裂案例、能在大规模真实数据上暴露有价值的信息（包括 Agent 自身的 bug）、且未对 `vmr report` 现有产物造成任何行为变化（除了顺带修复的那个预置缺陷）。可以进入第 2 步（缝合 + 行为剖面 + 一致性测试）。

---

## 附录 E：第 1 步代码评审整改记录（2026-07-29 完成）

对 `code-review-step1-2026-07-29.md`（附录 D 交付物的独立评审）列出的问题逐条复核，判断是否有效、是否值得修；修的都补了回归测试；跳过的记录理由。整改完成后对本轮全部改动（含评审报告本身指出的 `report` 包遗留缺口）做了一次全面走查，又额外发现并修复一个评审报告未提及的真实 bug（E.3）。全量测试（含 `-race`、`archtest`、CLI 端到端）通过。

### E.1 评审问题逐条处置

| 来源 | 问题 | 处置 | 理由 |
| --- | --- | --- | --- |
| §1.1 | system prompt 变更未被追踪为事件 | 不修 | 评审报告自己的分析已经说清楚：真实场景里 system 变更总伴随 lineage 分裂（`BrokeFrom` 已覆盖"这里发生了变化"），跨 lineage 的 system 对比只有第 2 步缝合后才有意义，留给 T2.3 |
| §1.2 | `PreviewTitle` 逐候选调用，`listJourneys` 无 `-journey` 时 O(候选数) 次全文件扫描 | **修** | 见 E.2.A |
| §1.3 | `eventHashAt` 对 system 消息的哈希方式与 `SysHash` 不一致 | 不修（已是既有状态） | 评审报告记录的是附录 D 走查时已经加过的注释说明，本轮未变 |
| §1.4 | `fetchFromFile`/`fetchRecordsFromFile` 无提前退出 | 不修 | 需要给 `audit.ForEachLine` 的行回调加返回值以支持提前终止——7 个调用点（`ctxgraph`×3、`report`×2、`replay`×2）都要跟着改签名，跨包改动，且 §1.2 修完后单文件全扫已经是"至多一次"，提前退出只优化"一次扫描内部提前停"这一层，收益进一步摊薄。留给第 2 步 |
| §1.5 | `BlobIndex` 仅记首次出现，缝合后可能回捞到旧 lineage 的内容 | 不修 | 修复需要 `FetchAll` 接受"lineage 时间范围"参数，而这个概念在第 1 步（无缝合）里不存在，属于第 2 步的范畴，第 1 步内提前做是过度设计 |
| §2.1 | `Ungrouped` 记录无排查入口 | **修** | 见 E.2.B |
| §3.x | 测试覆盖缺口（`responseSummary`/`sortByRootThenTime`/`renderStep` 等分支覆盖率） | 不修 | 评审报告自己的评估已经指出：`responseSummary` 的 SSE 分支实际被 `TestBuild_*` 系列间接覆盖，覆盖率工具计数口径有偏差；`ID`/`PreviewTitle` 0% 是 CLI 薄封装，真正逻辑在 `deriveID`/`deriveTitle` 里已覆盖。这些是覆盖率数字的解读问题，不是真实缺口 |
| §6.3 | 端到端路径缺测试：`-journey` 的完整 CLI 路径、golden test | **修** | 见 E.2.C、E.2.D |
| §5.1（"report 自身的缺陷"） | `buildFindings` 的确定性修复（附录 D.4）只有 `TestBuildFindingsIsDeterministic` 覆盖了 `worstWL` 一处 tie-break，另外三处（`worstTool`/`domModel`/`worst` 上下文膨胀）同样的 tie-break 逻辑完全没有回归测试 | **修** | 见 E.2.E |

### E.2 本轮修复

#### E.2.A `PreviewTitle` 性能：批量化 `FetchRecords`

`story.PreviewTitle` 内部调用 `ctxgraph.FetchRecords`（按源文件分组、每个文件最多打开一次），但 `listJourneys` 对每个候选 lineage 各调一次，等于把"按文件批量"的优化绕开了——候选数与源文件数往往不是一回事，候选数通常远多于文件数。

拆出 `story.PreviewTitles`（复数）：一次性收集所有待展示候选的根记录坐标，调一次 `ctxgraph.FetchRecords` 批量取回，再逐个提取标题；`titleFromRecord` 把"从已取回的 record 提取标题"这一步独立出来，`PreviewTitle`（单数）保留原签名，内部改为调用它。`listJourneys` 改为先过滤出真正要显示的候选（跳过断头且未 `-include-partial`），再对这批候选一次性批量取标题。

行为变化：批量取回失败时直接返回 error（不再退化为逐行显示"读取失败"）——由于这批文件在 `Scan` 阶段已经成功读过一次，同一进程内重新读取失败的概率极低，直接报错比静默降级更合理。

#### E.2.B `--show-ungrouped` flag

`vmr story` 原本只打印 `Ungrouped` 记录的计数，出现意外的未归组记录（例如新 agent 格式导致 SessKey 推导失败）时用户无法定位。加 `-show-ungrouped`，打印前 10 条未归组记录的 `path:line` + 时间戳。

#### E.2.C `cmd_story.go` 的 CLI 端到端测试

新增 `cmd/vmr/cmd_story_test.go`：`TestCmdStory_ListAndRender` 覆盖无 `-journey` 的列表路径（含批量标题）与带 `-journey` 的渲染路径（含文件写入、`0700`/`0600` 权限），`TestCmdStory_NoInputFiles`/`TestCmdStory_UnknownJourney` 覆盖两条错误路径。此前只有 `internal/story` 包内部对 `Build`/`RenderMarkdown` 的测试，`cmd_story.go` 本身的 flag 解析、候选列表、文件落盘从未被测试直接执行过。

#### E.2.D Golden test：`RenderMarkdown` 输出逐字节稳定

设计文档附录 C.3 验收标准 #4 明确要求的 golden test，此前一直没有——`internal/story` 现有测试（`TestRenderMarkdown_*`）只断言输出**包含**某些子串，不锁定整体结构；步骤编号错位、`<details>` 配对损坏、字段顺序变化都不会被现有测试发现。

新增 `internal/story/golden_test.go`：3 条记录的语料（纯文本应答 → tool_call/tool_result 配对 → 新用户指令开新任务）在 `goldenFixture` 里用 Go 代码构造、写入 `t.TempDir()`，而不是签入一份 `testdata/golden.jsonl`——`.gitignore` 对 `*.jsonl` 有仓库级排除（审计数据可能带真实对话内容，见其注释），本仓库其余所有测试 fixture 也都是同一惯例（代码构造 + 临时目录，从不签入原始 JSONL）。签入的只有 `testdata/golden.md`（渲染期望值）。`UPDATE_GOLDEN=1 go test` 重新生成 `golden.md`，人工 review 后提交；正常运行时逐字节比较。写这个 golden test 的过程中意外抓到一个真实 bug，见 E.3。

#### E.2.E `report` 包自身的测试缺口：`buildFindings` 确定性覆盖不全

附录 D.4 记录的 `buildFindings` 非确定性修复涉及四处"遍历全集合找最优 + tie-break"，但当时只给"定时任务冗余"（`worstWL`）补了 `TestBuildFindingsIsDeterministic`。另外三处——`worstTool`（工具 schema 浪费）、`domModel`（缓存未命中输入的 implicated 模型）、`worst`（上下文膨胀的 worst session）——用的是同一类 tie-break 逻辑，但完全没有测试守着，属于评审报告自己在 §5.1 指出、但未处理的缺口。

新增三个测试（`internal/report/aggregate_test.go`），各自构造一对精确打平的 fixture：

- `TestBuildFindingsWorstToolTieIsDeterministic`：两个工具 shape 各声明 5 个等长工具名、零调用，`SchemaWasteBytes` 精确相等，只有 `Shape`（`ToolsSig` 哈希）不同——胜出者用包内部的 `toolsSig` 现算，不硬编码哈希值；
- `TestBuildFindingsDomModelTieIsDeterministic`：两个模型各占全局 fresh token 总量的一半，`TokensInFresh` 精确相等，只有模型名不同；
- `TestBuildFindingsContextGrowthTieIsDeterministic`：两个 session 的 `ContextGrowth` 精确相等（均为 ×10），只有开场时间（进而 session ID `s01`/`s02`）不同。

每个测试跑 8 次 `Build()`，断言 tie-break 结果在所有运行中一致且指向预期一侧——手法与附录 D.4 的 `TestBuildFindingsIsDeterministic` 一致。

### E.3 评审过程中额外发现的问题（不在原评审报告范围内）

写 E.2.D 的 golden test 时，生成的 `golden.md` 里 Step 头部的 token 数显示成 `40ESTT/80ESTT/15ESTT`——`ESTT`/`ESTKT` 是 `internal/fmtutil.FmtTokens` 专门为**调用前的预估 token 数**（`core.RequestFacts.EstimatedTokens`，路由决策用）设计的单位后缀，其文档注释明确写着"必须让人一眼看出这不是已计费的实际用量"；`internal/report/detail.go` 甚至有一个平行的 `fmtTokensPlain`，专门为了在渲染**实际用量**数字时去掉这个后缀。但 `internal/story/render_md.go` 的 `renderStep` 直接拿 `fmtutil.FmtTokens` 去渲染 `m.Usage.In/CacheRead/Out`——这些是从响应里提取出来的**真实用量**，不是预估值，贴"EST"后缀等于说反了。

修复：`internal/story/render_md.go` 新增一个包内私有的 `fmtTokens`（同样的 K/M 定标，但不带 EST 后缀），替换掉 `renderStep` 里的 `fmtutil.FmtTokens` 调用，只保留 `FmtSeconds` 的复用（那个没有语义混淆问题）。这类"字段语义被格式化函数的命名/后缀覆盖掉"的错误，子串匹配式的单元测试（`TestRenderMarkdown_BasicStructure` 那种 `strings.Contains` 断言）天然发现不了——这也是 E.2.D 那条 golden test 真正的价值所在：不是为了满足验收标准清单，而是它确实抓到了一个子串测试抓不到的真实缺陷。

### E.4 结论

评审报告列出的 8 类问题里，2 类（§1.2/§2.1）+ 评审报告自己指出的 `report` 包测试缺口（§5.1）已修复并补齐回归测试；1 类（§6.3）修复了 CLI 端到端覆盖和 golden test 两个子项；其余 4 类（§1.1/§1.4/§1.5/§3.x）经复核后确认是第 2 步范畴或覆盖率数字误读，本轮不动，理由已记录在 E.1，供第 2 步开工时参照。修复过程本身又额外发现一个评审报告未提及的真实 bug（E.3，token 用量单位误标为"预估"），已修复。

全量测试通过：`go build`、`go vet ./...`、`go test ./...`、`go test -race ./...`、`go test ./internal/archtest/...` 全绿；`vmr story` 对新增的 golden fixture 跑了一遍真实 CLI（列表 → 渲染 → `-show-ungrouped`），输出与 golden 文件逐字节一致，产物权限 `0700`/`0600` 符合不变量。`internal/report` 生产代码本轮未改动（只加了测试），`vmr report` 现有产物不受影响，无需重新做 D.4 那种前后 diff 验证。

---

## 附录 F：用户反馈与后续增补（2026-07-29 完成）

附录 E 记录的是**评审驱动**的整改；本附录记录的是附录 E 之后、真实使用第 1 步产物时提出的**四条用户反馈**，均已实现，均不在附录 C.3 的原计划范围内，供第 2/3 步参照——尤其是 F.2（ID 格式）直接影响第 2 步 T2.2 缝合的设计（见附录 C.4 的标注），F.3 的并行化模式建议第 2 步的缝合/剖面计算也照搬。

### F.1 默认输入路径：从 `config.yaml` 的 `log_dir` 解析

**问题**：`vmr story`/`vmr report` 原来都要求显式传入文件/glob，但日志目录其实是固定的（`config.yaml` 的 `log_dir`），每次都要手打路径。

**方案**：新增 `cmd/vmr/auditpaths.go` 的 `resolveInputPaths`，两个命令共用：不带位置参数时，加载 `-c` 指定的 config（默认 `./config.yaml`），glob `${log_dir}/vmr-audit-*`（同时匹配 `.jsonl` 和压缩后的 `.jsonl.zst`）。带参数时行为不变。

**验证**：新增单元测试；用仓库真实 `config.yaml`（`log_dir: ~/data/vmr/logs`，含 `~` 展开）实测通过。commit `46b35db`。

### F.2 Journey ID 重新设计：client + 时间前置，哈希后置

**问题**：原 ID 是 `j-<12 位哈希>`，无法按时间/客户端排序，`ls reports/stories/` 看到的是一堆无意义哈希文件名，`-render-all` 之后几百个文件混在一起没法一眼分辨。

**方案**：改为 `j-<client>-<起始时间>-<结束时间>-<code>`。`client` 来自新增到 `ctxgraph.Manifest` 的 `ClientKeyTag` 字段（从 `audit.Record.ClientKeyTag` 带过来）；时间用 UTC、不带冒号的紧凑格式（`20060102T150405`，文件名安全且字典序即时间序）；`code` 是 `RootHash` 前 8 位，只用来给"同 client 同秒开局"这种极端情况消歧——身份判定仍然是完整的 `RootHash()`，没有变。

**影响**：`ls reports/stories/` 现在自动按 client 分组、组内按时间排序。真实语料（253 个候选）验证：`hermes`/`lobster`/`nokey`/`openclaw`/`pi`/`pimini`/`workbudy` 等按前缀清晰分组。golden test 同步更新。commit `e2686e1`。

### F.3 `-render-all`：批量渲染 + 并行化

**问题**：用户反馈"一个个挑 id 太麻烦了"，希望一次性把所有候选 journey 都渲染出来，不用先列表、再挑 id、再逐个 `-journey` 重跑。

**方案**：新增 `-render-all`，一次性渲染全部非断头候选（`-include-partial` 才含断头的）。

**性能问题与修复**：朴素实现（`BuildAll` 内部对每个候选串行调 `Build`）比纯列表模式慢了约 **15 倍**（真实 15 文件/253 候选语料上实测 3 分 38 秒 vs 15 秒），几乎全是串行瓶颈，用 `pprof` 定位到两处：

1. `ctxgraph.FetchRecords` 原本逐文件**串行**读，跟 `Scan` 早就有的并行 worker 池不一致；
2. `story.BuildAll` 组装每个 Journey（消息重渲染、事件去重、工具参数 JSON 美化）是单个 for 循环全部串行——而且这一步本身开销就不小，因为每一轮请求体带的是**累积的全部历史**、不是增量，单个 lineage 的组装开销是轮数的**平方级**。

把这两处都按 `Scan` 已有的 worker-pool 模式并行化后，同一份语料稳定复现到 **~65 秒**，多次运行输出逐字节一致（`-race` 验证无并发问题——`recs` 共享 map 在并行阶段只读，从不写）。commit `f783af7`。

### F.4 Step 渲染拆分为 Messages / LLM Response，展示完整 reasoning 和 tool_call

**问题**：用户核对一份真实渲染出的 journey 后发现，一个工具调用步骤只显示"🔧 调用工具: read, read"一行——没有参数、没有 id、没有 reasoning；而且这些信息要等**下一步**才会以"新进入上下文的消息"形式出现（因为它们变成 history 之后才被当作 event 渲染），比它实际发生的那一步晚了整整一步，读者得跨两个 Step 才能拼出"这一步到底做了什么"。用户同时问了 `lcp`/`cov` 这两个缩写是什么意思。

**方案**：
- `Step` 新增 `Reasoning` 字段——`chatmsg.StreamSummary.Reasoning` 此前被提取出来后从未使用，直接丢弃；
- `renderStep` 拆成两段：**Messages**（本轮新进入上下文的内容，不变）+ **LLM Response**（本轮模型自己产出的内容，当场完整展示：reasoning 单独折叠一块带字数、回复文本预览折叠、每个 `tool_call` 完整展示 name/id/美化缩进后的 JSON 参数，带 ` ```json ` 语言标签）；
- 顺带把编辑行的 `lcp=6, cov=75%` 缩写改成人话：`最长相同前缀 6 条消息，内容重合率 75%`（Journey 开头的断裂提示同步）。

**验证**：新增单元测试覆盖 LLM Response 渲染（reasoning + 多个 tool_call 的完整参数）；golden test 同步更新；用真实的、用户指出问题的那份 journey（`j-pimini-20260724...-aca17c1f`）重新渲染验证过，Step 4 现在完整展示了 reasoning 和两个 `read` 调用的完整参数。commit `afe3457`。

---

## 附录 G：第 1 步总体回顾与结论（2026-07-29）

### G.1 完成度核实

附录 C.3 的 4 个任务（T1.1–T1.4）全部完成；6 条验收标准里 4 条完全达标（#1 用等价方式验证、#4/#5/#6 直接达标），1 条（#2）实测数字优于要求，**1 条（#3：`tool_call`/`tool_result` 配对率 100% 的自动化断言）是本次核对中发现的唯一真实缺口**——附录 A.5 手工验证过一条真实记录的 57/57 配对，F9 的因果关系本身是协议给定、理论上不会错，但从未固化成一条持续跑在语料上的回归测试。"明确不做"清单的 8 项全部原样保留，附录 F 的四项增补没有一项提前踩进这条边界。

### G.2 与计划的偏离（汇总；逐项细节见附录 C.3 对应位置的标注）

| 偏离点 | 说明 |
| --- | --- |
| CLI selector | 只保留 `-journey`，未实现 `--session`/`--chat`/`--since`/`--until`（理由：`--session sNN` 用的是 `report` 自己的编号体系，与 `ctxgraph.Lineage` 的分组是两套独立系统，id 空间对不上） |
| 文件切法 | T1.3 把计划里的 `task.go`/`event.go`/`step.go` 三个文件合并进了 `journey.go`——`Build` 一次遍历同时产出 Journey/Task/Step/Event 四层结构，拆成三个文件需要在它们之间传递大量共享状态，合并后更简单 |
| 验收标准 #3 | `tool_call`/`tool_result` 100% 配对的自动化断言未落地（见 G.1），是唯一实质性的缺口 |
| D1 的 `t-` 前缀 | 断头 journey 的 id 应加 `t-` 前缀自证"不稳定"，实际未实现——断头与非断头 journey 目前用同一套 id 格式，只能靠正文里的断裂提示句区分（2026-07-29 补充核对，见附录 C.1 D1 的更新） |

### G.3 超出计划的增补

附录 F 的四项：默认输入路径（F.1）、Journey ID 重设计（F.2）、`-render-all` 批量渲染 + 并行化（F.3）、Messages/LLM Response 渲染拆分（F.4）——全部由真实使用中的用户反馈驱动，全部停留在 CLI 易用性与渲染质量层面，没有改变 §6.1 的概念模型或 `ctxgraph` 的核心数据结构（唯一的模型级改动是 `Manifest` 新增了 `ClientKeyTag` 字段，纯增量、不影响既有逻辑）。

### G.4 结论

第 1 步已达成设计文档 D.5 记录的验证目标（能正确切分 lineage、能复现已知断裂案例、能在真实数据上暴露有价值信息、未影响 `report` 现有产物），加上本轮的评审整改（附录 E）与用户反馈增补（附录 F），产物质量和易用性都比 2026-07-28 刚交付时更完整。**可以进入第 2 步**，但建议先把验收标准 #3 的缺口补上（成本很低，半天以内）——理由见附录 H：第 2 步的核心风险缓解策略直接引用了这条尚未自动化验证的假设。

---

## 附录 H：第 2 步范围核对与价值论证（2026-07-29）

### H.1 范围核对结论：不需要调整

对照当前代码逐项复核附录 C.4 的 T2.1–T2.5：目标文件（`internal/ctxgraph/stitch.go`、`internal/story/compaction.go`、`internal/story/metrics.go`、`internal/story/conformance_test.go`）全部尚未创建；`ctxgraph.EditKind` 目前仍只有 `Append`/`ReplaceTail`/`Contract`/`Fork` 四种，`Splice` 尚未拆出，`splice_or_tail` 混合桶原样保留在 `ReplaceTail` 里。第 1 步之后的四项增补（附录 F）全部停在 CLI/渲染层，没有触碰编辑分类或 lineage 结构。**原计划的范围、任务拆分、验收标准全部依然成立**，不需要重新规划——只有两处优先级/衔接点需要调整（已标在附录 C.4 对应任务后面）：T2.2 需要在开工时一并决定缝合后 Journey id 怎么取（附录 F.2 引入的新设计点），T2.5 建议提前到 T2.1 之前做。

### H.2 每个子任务的价值拆解

| 任务 | 直接用户价值 | 说明 |
| --- | --- | --- |
| T2.1 拆分 `splice_or_tail` | 低（间接） | 只是给 T2.3 的 `revision` 关系标注提供更准的判据，本身不改变任何渲染结果；范围窄，202 条边只占全部 6306 条边的 3.2% |
| T2.2 缝合 | **高** | 断裂只有 59 条边（0.93%），但高度集中在最长、最值得精读的会话上（附录 A.7：s290 444 请求断 10 次、s217 366 请求断 3 次……）——这些恰恰是当前第 1 步渲染体验**最差**的一批：越长的任务被切得越碎，最多切成 11 篇不相干的文件，读者得自己按文件名/时间手动拼 |
| T2.3 Compaction 三形态 | **高，独有** | 直接兑现 §5.1 论据 1 的"位置独有"——CCR 花 1600+ 行也修不好的问题，VMR 只需如实呈现"这次压缩把 42K token 压成 1.9K，其中 7 个文件路径消失了"。这是本功能相对 Agent 自身日志的核心差异化能力 |
| T2.4 行为剖面 | **高，且是文档自证的核心价值** | §3.2 拆解 deepseek 对比报告后得出的结论是"90% 的证据来自规则可得的表格"，并据此推翻了 v1.0"LLM 解读层是核心价值"的判断。这意味着**第 1 步交付的事实层（原始事件流）还不是价值的大头，剖面层才是** |
| T2.5 一致性测试 | 无（非用户可见） | 但是 R8（"Phase A 永久分叉，Phase B 不发生"）**唯一的缓解手段**，且已逾期（见附录 C.4 的优先级提醒） |

### H.3 ROI 评估：值不值得做

**成本**：原估 5 人天，建议加上补齐验收标准 #3 缺口的半天，共约 5.5 人天。

**收益**：
1. 让 16.1% 的多轮会话（附录 A.7）——尤其是其中最长、最有复盘价值的那批——从"被切成最多 11 篇互不关联的文件"变成一篇连贯叙事；
2. 交付文档自己论证过的"真正核心价值"（H.2 表里 T2.4 那条）——目前完全没有，用户拿到的只有原始事件流，没有任何规则可得的量化指标可看；
3. 解除一个**当前处于未缓解状态**的架构风险（R8），为第 3 步（`report` 迁移）铺路——没有这个测试，第 3 步"机械迁移"的承诺就没有安全网；
4. 剖面层做完之后，附录 C.6 的 4d（双 Journey 对比报告）几乎是免费的——这正是 §0 开头"三层动机"里排第二位的"横向对比不同 Agent 系统"，也是这整个功能最初的动机之一。

**值不值得做：值得，理由**：
- 这不是锦上添花，是设计文档自己的价值论证（§3.2/§5.1）指认的核心价值所在——按这个论证逻辑，第 1 步交付的"事实层"还不是主菜，第 2 步的"剖面层"才是；
- 59 条断裂边虽然数量少，但恰好集中在最长、最该精读的会话上，第 1 步对这批会话的体验反而最差，价值和当前体验的落差最大的地方正好是第 2 步要补的地方；
- R8 是真实存在、当前处于未缓解状态的风险，不是假设性的——拖得越久，新代码持续往 `ctxgraph` 里加、`report` 自己也可能继续演化，两边分叉只会越拉越大，第 3 步的迁移成本只会越来越高。

### H.4 成本与风险

主要风险是 T2.2（缝合）本身——**R1：把两个不相关任务错误缝成一个 journey**。缓解策略（原计划已写明）：宁可断开不要错连 + 每条边带置信度 + `same_chat` 默认不缝合，只标"疑似同源"。**这个缓解策略直接依赖 F9"Step 内因果零误差"的假设**——这正是为什么建议把验收标准 #3 的自动化断言（H.1/G.1 提到的那个缺口）作为第 2 步的前置任务：不补上，整个缝合功能的风险缓解论证就建立在一个从未被自动化验证的假设上。

次要风险是 F.2 引入的 ID 格式在缝合后如何处理（已在附录 C.4 T2.2 标注为需要在开工时定下来的开放设计点）。

**对 `report` 无回归风险**：第 2 步完全在 `story`/`ctxgraph` 内部，不触碰 `report` 生产代码——回归风险只会在第 3 步（迁移）出现，且第 3 步的安全网正是第 2 步交付的一致性测试。

### H.5 建议的执行顺序（相对原 T2.1→T2.5 顺序的微调）

1. **（新增，成本低）** 补齐验收标准 #3：`tool_call`/`tool_result` 配对率的自动化断言——半天以内，直接给 T2.2 的风险缓解论证补上验证依据；
2. **T2.5 一致性测试提前**——R8 已逾期，宜早不宜迟，建议放在 T2.1 之前或与之并行；
3. 其余按 T2.1 → T2.2 → T2.3 → T2.4 原顺序执行，任务定义和验收标准都不需要改。

### H.6 结论

第 2 步**值得做，范围不需要调整**，按 H.5 的顺序微调优先级后即可开工。

---

## 附录 I：第 2 步实施记录（2026-07-30 完成）

第 2 步（附录 C.4「能信 + 能比」）按附录 H.5 的优先级顺序（前置补课 → T2.5 → T2.1 → T2.2 → T2.3 → T2.4）已实现并通过全面代码走查 + 真实语料验证，记录如下，供第 3 步参照。过程记录（含每个子任务的即时进度、真实语料数字、bug 修复经过）保留在开发期间的 `_tmp/plan_Sonnet5.md`（任务完成后按惯例删除，本附录是它的固化摘要）。

**开工前的 E.3 复核**：本轮开工前按要求重新评估了附录 E.3（token 用量单位误标为"预估"的渲染 bug）——`internal/story/render_md.go` 现在只在 `fmtutil.FmtSeconds` 上复用共享格式化函数，`fmtTokens`（包内私有，无 `EST` 后缀）仍是唯一渲染实际用量的路径，全包搜索确认无残留的 `fmtutil.FmtTokens` 误用。**结论：E.3 已修复且无回归，本轮无需改动**，作为第 2 步开工前置检查的一部分予以确认并记录于此。

### I.1 交付物

- **前置补课**：`internal/chatmsg/pairing.go`（`CheckToolPairing`，补齐第 1 步验收标准 #3 遗留的自动化缺口）。
- **T2.5 一致性测试**：`internal/report/session_conformance_test.go`（设计文档原计划的路径 `internal/story/conformance_test.go` 因 archtest 边界不可行，改放 `report` 侧，与既有 `ctxgraph_parity_test.go` 同一惯例，已在文件头注明）。
- **T2.1 拆分 `splice_or_tail`**：`internal/ctxgraph/edit.go` 新增 `Splice EditKind` + `commonSuffixLen`。
- **T2.2 缝合**：新增 `internal/ctxgraph/stitch.go`（`StitchGraph`/`ChainFrom`/`LineageIndex`/`StitchedSuccessorSet`），`internal/story`（`journey.go`/`candidates.go`/`preview.go`/`render_md.go`）与 `cmd/vmr/cmd_story.go` 全面改造为 chain-aware——Journey 从"1 lineage"泛化为"1 缝合链"。
- **T2.3 Compaction 与 revision**：`Step.SysChanged`/`Step.Compaction`（`CompactionInfo`）/`Event.Revises`，均在 `internal/story/journey.go`，渲染在 `render_md.go`。
- **T2.4 行为剖面**：新增 `internal/story/metrics.go`（`ComputeMetrics`/`Metrics`/`JourneySummary`），`Step.HumanInitiated` 字段，`cmd_story.go` 的 `writeJourneyFile` 现在同时写 `journey-<id>.json`。
- 全部新增/改动文件均有随附单测；`internal/archtest` 的边界规则未变（`ctxgraph`/`story` 的依赖边界在第 1 步已经建立，第 2 步没有新增依赖方向）。

### I.2 关键设计决策（自主决策，均按"debrief 后无阻塞问题即自行实施"的指示执行）

- **D-ID（缝合后 Journey id 取法）**：取整条链最早 lineage 的 client + code，覆盖到最晚 lineage 的结束时间——`deriveID` 从"单 lineage"泛化为"lineage 链"，解决了 T2.2 任务里标注的开放设计点。
- **D-Splice 判据**：`Splice` = `ReplaceTail` 形状的边里，prev 尾部至少 `spliceMinTailMatch`(=2) 条消息原样重现在 cur 尾部末尾；不满足则维持 `ReplaceTail`。`Splice` 和 `ReplaceTail` 一样不分裂 lineage。
- **D-Stitch（缝合判据）**：blob 倒排索引找每个断裂 lineage 首 manifest 的最佳覆盖前驱；design doc 原定的四类边（splice/compaction/head_prune/same_chat）收缩为三类——`ctxgraph` 坚持"无模板匹配"的包边界，splice 与 compaction 的区分依据（可选的摘要请求信号）在这一层拿不到，因此合并；`same_chat` 默认不缝合，只标"疑似同源"（`AmbiguousMatch`）；三态处理（已缝合/确认无后继/疑似同源）对应设计文档 F4。
- **跨 SessKey 时间窗（`stitchCrossBucketMaxGap`，6 小时）**：真实语料验证中发现的必要修正，见 I.3。

### I.3 真实语料验证与过程中发现的问题（全部 15 文件 / 7112 条记录 / 226 个非断头候选 journey）

- **T0**：`tool_call`/`tool_result` 配对率 `406534/406534`，零孤儿——第 1 步验收标准 #3 的缺口补齐，R8/R1 的风险缓解论证不再建立在未验证的假设上。
- **T2.5**：752 个 report session 与 ctxgraph lineage 分组逐条比对，718 个 1:1 对应，34 个被 ctxgraph 正确切成多段（F6 模式，`knownImprovement` 白名单），**0 个反向违规**（无 lineage 横跨多个 report session）。
- **T2.1**：全语料边分布 `append 96.25% / replace_tail 2.68% / splice 0% / contract 0.43% / fork 0.65%`。诚实记录：`Splice` 命中数为 0——F11 附录 A.4 的真实 S2 案例（s231 turn20→21）走的是 `Contract` 分支（大幅缩减），根本进不了"ReplaceTail 形状"的候选池；`Splice` 字面判据忠实实现了设计公式，只是这个模式在当前语料的"未大幅缩减"边里没有真实样本。真正的 S2 类案例已经在 T2.3 的 revision 关系检测里独立处理（详见 I.3 的 T2.3 小节和 `Event.Revises` 的判据）。
- **T2.2（关键发现）**：初版缝合（无时间窗）在语料上跑出"太顺利"的 68/68 全部缝合、0 无匹配——深挖后发现 5 个跨 SessKey 匹配置信度很高（0.69–0.84）但实际相差 196.7 小时，是"每天重复出现的定时任务模板文本"造成的巧合高分。修复：新增 `stitchCrossBucketMaxGap`（6 小时，仅限跨 SessKey 匹配）。修复后重新验证：68 个断裂中 **62 个成功缝合**（24 compaction + 38 head_prune）、**6 个正确识别为"疑似同源"未缝合**、**0 个跨桶时间窗违规**——"宁可断开，不要错连"被实测数据验证生效。端到端测试额外抓到一个真实 bug：缝合边界的任务标题会把"前驱段已经出现过的共享开场白"误认成新指令，导致标题把开场指令重复一遍；修复为 `newInstructionTitleAtStitch`（跳过全局 `seen` 里已出现的候选）。
- **T2.3**：用真实缝合出的 `j-hermes-20260722T144801-20260725T185800-ab22369f` 复测，`t03` 缝合边界正确显示 `⚙️ system prompt 变更` + `📉 信息损失: 34.7K → 35.0K tokens（101.0%）· 0 个实体消失 / 30 个存活`，实体列表正确抓到 `HERMES.md`/`AGENTS.md`/`https://hermes-agent.nousresearch.com/docs`。
- **T2.4**：`vmr story -render-all` 全量渲染 226 个 journey，5m30s 完成，0 panic，226 份 `.md`+`.json` 全部生成且逐一 `json.Unmarshal` 通过；跨全部剖面核对分布计数/token 占比求和/比例类字段值域/时间字段非负，仅 1 类预期内的良性边界（某工具调用参数短到估算 0 token 时该项 `token_share` 按定义停在 0，不是除零 NaN）。**精确复现设计文档 A.6/F10 的手算结果**：s231（真实语料里 24 请求的原始会话，缝合后对应 `j-openclaw-20260716T071729-20260716T075153-71c91f41`）—— `ComputeMetrics` 算出模型时间 380.983s / 占比 18.40%、Agent 侧间隙 88.276s / 占比 4.26%、墙钟 2070.24s，与设计文档手算的「381.0s/18.4%、88.0s/4.3%、2069.9s」几乎逐位吻合（差异在亚秒级，属于测量粒度噪声）。这同时验证了 T2.2 的缝合（s231 本身在 ctxgraph 里天然是两段 lineage，缝合后才重新变回手算时依据的完整会话）与 T2.4 的 F10 时间三分解逻辑。

### I.4 验收标准核对（对照附录 C.4 原文四条）

1. **`vmr report` 的 compaction linking 告警全部落入"已缝合"或"确认无后继"，无"匹配失败"** —— ⚠️ **部分验证**。当前语料（已比设计文档写作时增长）上 `vmr report` 打出 5 个不同时间点、12 行告警（不再是文档原写的 8 条，语料范围扩大所致）。抽查其中最明确的一个——`2026-07-16T15:51:15`（即 F2/F6 反复引用的 s231→s238 案例）——精确对应 ctxgraph 的 lineage 251（`break_kind=contract`），且 `stitch_outcome=stitched`（`head_prune`，score 0.33，pred=lineage 250），落入"已缝合"，符合标准。其余 4 个时间点因为 `report` 的 needle 匹配与 `ctxgraph` 的 SessKey/anchor 分组本就是两套独立的会话切分逻辑，逐点精确对齐需要复刻 `report` 那套已经被判定为"有缺陷、留给第 3 步替换"的旧启发式，性价比低——**T2.5 的一致性测试是这个问题更严格、更全面的版本**（752 个 session 全量比对，而不是抽查几条旧日志），已经证明 ctxgraph 的分组不劣于且多处优于 report 的现有分组。第 3 步 `report` 迁移到 `ctxgraph` 之后，这条旧日志本身就会被替换掉，不需要再单独验证。
2. **s231 的两段被缝成一个 journey，边类型 `compaction`，置信度高，证据可打印** —— ⚠️ **部分符合，已如实记录差异**：s231 确实被缝成一个 24 轮的连续 journey（`j-openclaw-20260716T071729-20260716T075153-71c91f41`），证据可打印（`🧵 缝合自更早片段`标记）；但边类型实测是 `head_prune`（score 0.33）而非 `compaction`——`resolveStitch` 的判据是 `l.BrokeFrom.Edit.Kind == Contract && bestScore >= stitchCompactionScore(=0.5)` 才归 `StitchCompaction`，s231 的 break 确实是 `Contract`，但 blob 覆盖率只有 0.33，够不到 0.5 的门槛，落进 `StitchHeadPrune` 而不是原文预期的 `compaction`。这不是 bug——0.33 的覆盖率如实反映了"这次断裂前后内容重合度确实不算高"，`stitchCompactionScore` 的取值本身没有用更大语料重新校准（stitch.go 的阈值注释已标明"initial values, not yet corpus-recalibrated"）。留给后续用更大语料校准阈值时复核，不阻塞第 2 步交付。
3. **对 §4 F1 的 6 个会话各出一份剖面，人工核对净工作时长与 A.6 的手算一致** —— ✅ **s231 精确验证通过**（见 I.3），其余 3 个 F10 表里有手算值的会话（s187/s634/s407）未逐一重复核对（`ComputeMetrics` 是同一套代码路径，无理由对不同会话产生系统性偏差；s231 的近乎逐位吻合已经是最强的单点证据）。原文列出的 6 个会话里 s290/s217 本身不在 A.6/F10 的手算表格里（表格只手算了 4 个），无手算基准可比对。
4. **一致性测试通过，且 `knownImprovement` 白名单里每条都有 F 编号** —— ✅ 完全符合（I.3 的 T2.5 小节）。

### I.5 代码评审结论

对全部新增/改动文件做了一次逐文件走查（`internal/ctxgraph/{edit,lineage,stitch}.go`、`internal/story/{journey,candidates,preview,render_md,metrics}.go`、`cmd/vmr/cmd_story.go`、`internal/chatmsg/pairing.go`、`internal/report/session_conformance_test.go` 及全部对应测试文件），未发现新增问题；`go build`/`go vet`/`go test ./...`/`go test -race ./...`/`go test ./internal/archtest/...` 全绿。

### I.6 明确不做 / 留给后续

- `stitchCompactionScore`/`stitchHeadPruneScore` 等缝合阈值仍是第 2 步的初始取值，尚未像 `edit.go` 的 `contractLenRatio`/`forkCoverage` 那样用更大语料重新校准（I.4 第 2 条已记录一个具体例子）；
- T2.4 的错误恢复次数（`ErrorRecoveryCount`）只识别 Anthropic 的 `is_error` 字段，OpenAI 协议无对应标准标记，纯 OpenAI 语料下会偏低估——如实记录的已知局限，不是 bug；
- 第 3 步（`report` 迁移到 `ctxgraph`）未开始，`report` 自身的 `compaction linking`/`context_growth` 缺陷仍然存在，按原计划留给第 3 步。
