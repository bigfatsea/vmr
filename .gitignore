# Ver 2026-07-10 00:00, by Sonnet 5

# Binaries
/vmr
/bin/
*.exe

# Local config with real keys (use config.example.yaml as template)
/config.yaml
/config.local.yaml

# Test & coverage
*.test
*.out
coverage.html

# Scratch
/_tmp/

# OS / editor
.DS_Store
.idea/
.vscode/

# Generated reports (details/ carries full conversation bodies — never commit)
/vmr-report.json
/vmr-report.md
/vmr-requests-index.md
/details/

# Audit data (may contain full conversation bodies — never commit)
*.jsonl
*.jsonl.zst

# Runtime logs (vmr.sh daemon + audit) and image-downscale cache — only
# populated here when $TMPDIR is unavailable (internal/rundir's last-resort
# fallback); the normal default lives under the system temp dir instead.
/logs/
/image_cache/
